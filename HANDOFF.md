# Pacemaker Navi 引き継ぎ（2026-09-10 更新・追補53時点）

> **メーカーの表示順（2026-09-11）**：新しい画面のメーカー順を **Abbott → BIOTRONIK → Boston Scientific → Medtronic → MicroPort CRM（上位5社は固定）→ それ以外（旧社など）はアルファベット順** に変更（`MAKER_ORDER`／`makerCmp`）。絞り込みチップ、一覧の並び、MRI、メーカー比較の5社の行、編集フォームの欄の順すべて。アルゴリズムの「他社比較」は、メーカー名の付いた行だけをこの順に並べ替えて表示し、メーカー名のない補足行は元の順のまま最後に置く（データの文章は変更しない）。従来の画面（classic.html）は元のまま。
> 表示確認テストは、他社比較だけ行単位で照合する方式に変更した（行の順番を変えて表示するため）。
> **読みでの検索・入力候補・拡大の停止・アイコン（2026-09-11）**：英語の製品名・機能名をカタカナ・ひらがなでも引けるようにした（例：アジュール／あじゅーる＝Azure、オートキャプチャー＝AutoCapture）。**英単語 → 読み の辞書 `readings.json`（1,398語）を index.html に埋め込み**（検索専用。データの内容は変えない）。大文字の略語は1文字ずつの読み（MRI＝エムアールアイ）。ひらがな／カタカナ、長音、小さい文字の違いは区別しない。名前の読みでの一致を、別名・型番などの読みでの一致より上位に出す。
> **データに新しい英単語が増えたら、読みの辞書に追加する**（`PNV2.missingReadings()` で読みのない語を一覧できる。辞書の元は `readings_1〜5.txt`、組み立ては `build2.py`）。
> 文字を打つと候補（最大8件）が入力欄の下に出る（ホーム・検索・各一覧・MRI の本体）。ホームでは候補のみを出し、Enter または「検索結果をすべて見る」で検索画面へ。
> 画面の拡大を止め（viewport と touch-action、iOS の gesture イベント）、スクロールの端での揺れを止めた（overscroll-behavior）。入力欄・選択欄の文字はすべて 16px 以上（iOS の自動拡大を防ぐため）。メーカー比較の「＋ 機能を追加」を削除（詳細画面からの編集は残す）。
> **アプリのアイコンをユーザー提供の画像に変更**（icon.png＝iPhone 用 180×180、角の黒い部分を背景色で埋めたもの／icon-192・512＝角を透明にしたもの／icon-maskable-512＝背景を広げて絵柄を安全域に収めたもの）。
> **下部タブ（2026-09-11 確定）**：**下部タブは「ホーム｜検索｜履歴｜保存｜メニュー」で固定（ユーザー指定の仕様）。**一度「ホーム｜用語｜製品｜アルゴリズム｜MRI」に変えたが、依頼の読み違いだったため元に戻した。用語・製品・アルゴリズム・メーカー比較・MRI・年表へはホームの6つの入口から入り、入口から入り直すと前回の絞り込みを引き継ぐ。各画面の左上に戻るボタン。右上のアイコンは置かない。
> **用語・メーカー比較の追加・編集・削除（2026-09-11）**：従来の画面と同じ localStorage キー `cieds_overlay`・同じ形式を使う（どちらの画面で変更しても両方に表示。別タブの変更も storage イベントで反映）。公式データ（data.json）は変えない。編集済みの項目は「この端末で編集」と表示し、詳細画面で「元の登録内容（公式データ）」を確認・取り消しできる。メニューに変更の一覧・元に戻す・書き出し・読み込み・すべて元に戻す。用語一覧：すべて／保存／最近見た、対象デバイス、カテゴリ、並び替え、＋ 用語を追加。アルゴリズム：メーカー別｜メーカー比較の切り替え。MRI：メーカー → 種別 → 本体の順に絞り込み。件数表示は公式データの件数。
> **v2 への切り替え＋PWA 化（2026-09-11）**：`github_upload/` の7ファイルでリポジトリを更新する。`index.html`＝新しい画面（v2・オフライン対応）、`classic.html`＝切り替え前の `index.html`（バイト単位で同一）、`sw.js`＝Service Worker、`manifest.webmanifest`＋`icon-192/512/maskable-512.png`＝ホーム画面追加用。既存の `icon.png` はそのまま使う（iPhone のホーム画面アイコン）。
> **オフラインの方針**：通信できるときは毎回最新を読み（ネットワーク優先、4秒で打ち切り）、読めないときだけ端末の保存分を表示し、ホームに「オフライン表示中（取得日時）」を出す。保存データは元の JSON とバイト単位で同一であることを確認済み。
> **`sw.js` の `VERSION` は、保存するファイルの一覧を変えたときだけ上げる**（JSON や index.html の更新だけなら変更不要。ネットワーク優先なので自動で新しい版が保存される）。
> 切り替え後、方針8・12（`F_FALLBACK`／`G_FALLBACK` の同期）は `classic.html` に対して行う。v2 の辞書（`SYN`・`CONCEPT_MAP`）は `classic.html` から複写したもの。リポジトリの `v2.html` は切り替え後に削除してよい。
> **UI 試作 v2（2026-09-10）**：新しい UI を **`v2.html`（別ファイル）** として作成した。`index.html` と JSON 4ファイルは変更していない（SHA-256 一致を確認）。
> `v2.html` はリポジトリのルートに置くと同じ JSON を読んで動く（`https://ori287.github.io/pacemaker_navi/v2.html`）。JSON 同梱の単体版 `Pacemaker_Navi_v2_preview.html` もある。
> 全2,526件・38,999項目が詳細画面に表示されること（欠損0）、MRI 判定が現行版と一致すること（677通り・不一致0）を機械確認済み。詳細は `UI_V2_REPORT.md`。
> **v2 は `index.html` から `SYN` と `CONCEPT_MAP` をビルド時に複写している。辞書を変えたら v2 も作り直す。**v2 の端末内保存は `pn2_` で始まるキーだけを使う（`cieds_*` には書かない）。
> **このときは JSON を一切変えない方針だったので、notes.json には追記していない。**次にデータを更新するときに、notes に「UI 試作 v2」の記録を1エントリ足す。
> **v2 では、データを変えたら確認すること**：新しいフィールドを足した場合、詳細画面の「そのほかの登録項目」に自動で出るので表示漏れはないが、しかるべきタブに載せるには `v2.src.html` の対応表（`PROD_LABEL` など）への追加が要る。

## ▼ 次のトークで最初に読むところ

**渡すファイル**：`HANDOFF.md`（このファイル）＋ `algos.json` `data.json` `products.json` `mri.json` `notes.json`。
`index.html` は改修が要るときだけ渡せばよい（466KB あるが会話には載らない）。
**追補40で `index.html` を改修した**（製品タブの `CAT_ORDER` に「スリッター・ツール」を追加）。
**追補43で `index.html` の `G_FALLBACK` を修正し、追補44で5語、追補45で10語、追補46で2語、追補47で4語、追補48で17語、追補49で11語を同期した**
（`CAT_ORDER` と `F_FALLBACK` は変更なし。**製品用 `CAT_ORDER` には「CRT-P」が既に入っているので、追補49の CRT-P 登録でも HTML 改修は不要だった**）。
**手元の `index.html` が追補40より古いと、スリッター2件が分類の末尾に出る。**
**JSON は何個渡しても会話容量を食わない。`.md` は全文が載るのでこのファイルだけにする。**
**追補53で `index.html` を更新した**（製品用 `CAT_ORDER` の末尾に「ICM付属品」を追加／`G_FALLBACK` の5語を同期。478,305 → 482,498バイト）。**手元の `index.html` が追補53より古いと、ICM付属品2件が分類の末尾に五十音順で出る。**

**直前のトークで終わったこと（追補53）**：BIOTRONIK「BIOMONITOR IIIm ～製品トレーニング資料～」（日本語・スキャンPDF・76スライド）を反映した。**4タブすべてを更新した。手書きは一切採用していない。**
**アルゴリズム 204 → 214件、用語集 1219 → 1247語、製品 939 → 943件（本体 42 → 44・関連機器 27 → 29）、MRI 56 → 57グループ（verify 19 → 20）。**
**ICM のアルゴリズムはこれまで全社で0件だった。**資料の見出しごとに BIOTRONIK の10件を起こした（分類 `cat`＝「ICM」、`device`＝「ICM」）：
`BIO-ICM-AF-DETECTION`（AF sensitivity Low/Medium/High のプリセット表、Window size 8/16・16/24・24/32、RR variability limit 6〜18%、Detection intervals 5〜23、Number of detection windows 1〜4、Confirmation time 1〜30min、Termination intervals 1・3・5・7、Number of termination windows 1〜4、**AF detection enhancement（観察・確認フェーズ中の3分間のみ。キャンセルされるとエピソードは保存されない）と AF with ectopy detection algorithm（閾値は確認できない）を同じエントリに収めた**）／
`BIO-ICM-HIGH-VENTRICULAR-RATE`（**増減カウンタ**・停止は5連続・HVR counter 8〜40）／`BIO-ICM-BRADYCARDIA`（Brady duration 5〜30s・停止は10連続）／`BIO-ICM-SUDDEN-RATE-DROP`（Baseline 256/64/48・Rate-drop 32/16/8・50%・**終了条件なしでトータル40秒**）／`BIO-ICM-ASYSTOLE`（2〜10s・トータル40秒）／
`BIO-ICM-PATIENT-TRIGGER`（Remote Assistant III・前7分＋後30秒）／`BIO-ICM-SENSING`（Sensing filter 4.5/10/15/24Hz・SensingConsult 5パターン〈Standard＝ピーク値の62.5%→25%〉・SECG Signal filter 0.05/0.5Hz・ノイズウィンドウ 100ms と Vn）／
`BIO-ICM-INTELLIGENT-MEMORY-MANAGEMENT`（3バッファ・56エピソード60分・上書き保護15＋4）／`BIO-ICM-PROGRAMCONSULT`（Syncope／Palpitations／AF monitoring／Cryptogenic stroke の全値）／`BIO-ICM-HOME-MONITORING`（最大6件/日の優先順位・**60日間送信がないと伝送中止なのに表示は ON のまま**・定期SECG）。
**製品**：`BIO-D-BIOMONITOR-IIIM`（5.5年*・2021年2月販売開始・販売名バイオモニター3／30100BZX00153000）と `BIO-D-BIOMONITOR-III`（4年*・2020年1月）を分類「ICM」で登録。**償還価格は空欄**（標準型 394,000円／特殊型 451,000円のどちらか資料が明記していない）。
関連機器に `GEAR-ICM-1` Remote Assistant III・`GEAR-ICM-2` FIT Onestep／切開ツールを新分類「ICM付属品」で追加し、`GEAR-TX-3`／`GEAR-RM-3`／`GEAR-PRG-3` に加筆。
**MRI**：`BIO-ICM1`（1.5T／3T・円筒型ボア・スルーレート 一軸200T/m/s以下・SAR 全身4.0／頭部3.2W/kg・胸部に局所送受信コイル不可・MRIカードは発行されずICM手帳で確認）。**撮像条件の章は BIOMONITOR III 表記なので verify を付けた。**
**用語集**：新規28語（末尾に追加）、`BIOMONITOR IIIm` を全面書き直し（旧記載の「大きく明瞭なR波感知」は資料になく削除）、ほか6語に加筆。
**あわせて指示を受けて ERAF の誌名「Circulation」を削除した**（用語 `ERAF`・`BIO-POST-MODESW-RATE`。誌名は私の同定で資料の記載ではなかった）。**§6 の古い3行（HAR limit の換算・Far-field protection の理由・Scan decrement の類推）も、アプリ側が追補52-3で直っていることに合わせて整理した。**
**追補52-4 の数値例7箇所は、本資料のどれにも該当しなかった**（引き続き保留）。
**未確定13件は notes 追補53 と §6 に記録した。**とくに **Confirmation time（表は6、Cryptogenic stroke の注記は「1 ⇒ 2」）**、**HVR counter（図は4、画面の選択肢は8〜40）**、**患者トリガ（7分30秒 対 早見表の310秒/件）**、**Palpitations テンプレート（表と HM 画面例で Transmission が違う）**の4つは資料内の食い違い。

**その前（追補52）に終わったこと**：BIOTRONIK「Biotronik Product Brady  Closed Loop Stimulation」（日本語・スキャンPDF・22スライド）を反映した（追補52）。
**アルゴリズム 204 → 209件、用語集 1199 → 1219語、製品は 939件のまま3件を加筆。MRI は変更なし。**
**`BIO-CLOSED-LOOP-STIMULATION` を全面的に書き直した**（追補49時点は2行の説明に加速度センサの設定が同居しているだけだった）。
**循環制御の閉鎖ループ**（循環中枢 →〔クロノトロピー→洞結節→心拍数〕と〔イノトロピー→心筋→収縮力〕→ 心拍出量 → 平均動脈圧 → 圧受容器 → 循環中枢）、
**洞不全症候群でクロノトロピー側が欠落した「不完全閉鎖ループ」をペースメーカで再構築するのが CLS** という位置づけを収載。
**測定は RV tip to Can（unipolar）、電極周辺 約1cm² の心筋の状態に依存する抵抗値測定。**
**根拠は単極インピーダンスと dp/dt の相関**（ドブタミン負荷。Intrinsic R²＝0.9252／VVI paced R²＝0.9247。PACE 2000;23(Pt II):1502-1508）。
**レートへの反映は「面積の差が大きいほど上昇」。**

**設定は4項目**：**Max. CLS rate**（目標とする最大センサーレート。上限を決めるのはこの項目。画面例 120bpm）／
**CLS response**（**ターゲットレート＝BR＋[（MCLR−BR）×係数]、係数 0.21／0.26／0.33／0.41／0.51、ノミナル Medium。レート分布を80%にする機能であって上限は変えない**）／
**CLS resting rate control**（+20ppm、Off・+10〜+50ppm、10ppm刻み。**神経調節性失神では OFF もしくは 100ppm 程度まで上昇可能に設定することを推奨**）／
**Vp required**（No＝延長して自己伝導優先／Yes＝延長せず Vp 優先。**59サイクル → 60・61拍目に延長 → 4セット8拍中6拍で移行／8拍中7拍連続 Vp でキャンセル／180拍連続 Vp で再評価**）。
**リファレンス**は測定期間 50〜300ms・16発、安静時にのみ取得、Vp 用は AV ディレイを2拍短縮して取得、**Vp required が Yes だと Vs リファレンスは更新されない。**
**CLS fusion avoidance zone** は常に On で、**AV ディレイ直後 40ms に Vs があると +1／なければ −1、最新16拍中10拍以上でリファレンス取得中止・CLS 休止。**
**これらは当初5件の独立エントリにしたが、追補52-2の指示で `BIO-CLOSED-LOOP-STIMULATION` に統合した（209 → 204件）。**

**既存6件を訂正／加筆**：`BIO-VP-SUPPRESSION`／`BIO-INTRINSIC-RHYTHM-SUPPORT`／`BIO-POSITIVE-AV-HYSTERESIS-WITH-SCAN-REPETIT`（下記の Vp suppression の代替の件）／
**`BIO-DYNAMIC-AV-DELAY`**（自己 PQ 間隔より設定 AV ディレイが長い場合は Vp のリファレンス波形が作成できないためダイナミック AV ディレイを推奨しない）／
**`BIO-ACCELEROMETER`**（CLS の安静判定に使われている関係＋センサ比較表）。

**`index.html` を更新した（方針12）。****訂正した「CLS」が `G_FALLBACK` の698語に含まれていたため同期した**（追補50・51・51-3では該当なしだった）。件数は698のまま。
**`feats` 未変更のため `F_FALLBACK` の同期は不要。**

**そのあと（追補52-4）に指示を受けて、アルゴリズム全204件を対象に推測で書いた記述を洗い出し、9箇所を訂正した**（用語集2語も）。
指示の原文は**「これまでのアルゴリズムで推測で記載したものはありますか？それらも全て訂正してください。」**
**推測を示す語23種と方針13の4類型に対応する語で全件・全フィールドを機械走査し、のべ50フィールドを1件ずつ確認した。**
削ったのは、`ABT-DYNAMICTXTM-OVER-CURRENT-DETECTION` の「治療成功と読める」、`ABT-SVT-UPPER-LIMIT` の Off の意味の推論、
`ABT-VT-THERAPY-TIMEOUT` の「秒単位と分単位のどちらでも設定できるとみられる」、`ABT-PVAB` の設計意図の推定、
**`ABT-ANTITACHYCARDIA-PACING`／`ABT-SHOCKGUARDTM-DETECTION` の「375×0.85＝318ms／Min BCL 320ms」という私が作った数値例**、
`ABT-RATE-BRANCH` の Slow VT の臨床例、`ABT-BIGEMINAL-AVOIDANCE` の「200ms と 500ms」、
`ABT-I2I-LINK-EVALUATION` の「100件送ったとき」、`BIO-RATE-FADING` の場面設定。
用語集は **`最小ペーシングインターバル（160ms）` の「約375bpm に相当する」**と **`HAR limit` の「（＝200bpm）」**を削除した（どちらも私の単位換算）。
**判断に迷って残した8箇所（資料由来と判断した数値例7つと、一般的な臨床の目安1つ）は、理由とともに notes 追補52-4 に書き出してある。**
**元資料が手元にないため資料に載っていたかを確定できていないので、次に各資料が来たときに突き合わせる。**
**あわせて方針13の④を実運用に合わせて書き直した**（「資料にある動作・所見の読み方の案内」は残し、「資料にない事実・数値・手順の創作」を禁じる形にした）。

**そのあと（追補52-3）に指示を受けて、用語『CLS 使用時の心室ペーシング抑制機能』を削除し（1220 → 1219語）、
私が推測で書き足していた記述を21箇所洗い出して削除・書き換えた。**
指示の原文は**「推測ではなく、資料にある情報を読み取って反映していってください。」**
**方針13「推測を書かない」を §2 に明文化した。**削ったのは、HAR limit の ms→bpm 換算、CLS response の計算例、
Vp required のノミナルの推定、CLS の「疑う順序」、リード抵抗値の 1,200Ω の例、Scan decrement の他社からの類推、
ブリッジペーシングの運用助言など。
**とくに `BIO-HVR-DETECTION` の「心房 190bpm の AT が 1:1 伝導すれば…」は、追補51で自分が「手書きなので不採用」と書いた内容を
言い換えて復活させてしまっていたもので、削除した。**

**そのあと（追補52-2）に指示を受けて3点直した。**
**① 新規5件を `BIO-CLOSED-LOOP-STIMULATION` に統合**（209 → 204件。`how` を5つの見出しに区切って全内容を収めた。削除した5 ID への参照が0件であることを機械確認済み）。
**② 「CLS の代替は IRS plus もしくは Positive」という私の書き方が誤りだったので訂正した。**
**資料が言っているのは「Vp suppression（心室ペーシング抑制機能）の代わりに何を使うか」であって、CLS の代替ではない。**
CLS を使うと Vp suppression が使えなくなる → **それでも心室ペーシングを減らしたいなら Vp suppression の代わりに IRS plus か Positive を置く**、という構図。
関係する4エントリ・用語集2語・製品3件を直し、**用語集に『CLS 使用時の心室ペーシング抑制機能』を追加**して誤読しやすい点を独立させた（1219 → 1220語）。
**③ Amvia シリーズにも CLS はある**ので、追補52で付けた「4項目が Amvia Sky にもあるかは未確定」という但し書きを削除した。
**追補49の時点で Amvia Sky に DDD-CLS／DDI-CLS があることは確認済みだった**（『AOO』廃止・『OFF』追加も）。
**資料が E シリーズを対象機種として挙げているのは、その世代を例に説明しているためで、Amvia Sky に CLS がないという意味ではない。**
`models`・製品3件の `diag`・用語集2語を直し、**§6 の未確定から1件を削除した。**

**そのあと（追補51〜51-3）に終わったこと**：BIOTRONIK「Amvia Sky シリーズ DR-T/HF-T QP/HF-T BP “More Options for Device Management” Atrial ATP」（日本語・スキャンPDF・24スライド）を反映した（追補51）。
**追補50で反映した2スライドは、この資料の抜粋だったと判明した。**追補50は「Atrial ATP の設定」の章の2枚だけで、本資料はその前段の理論・フローチャート・作動制限をすべて含む完全版である。
**アルゴリズム・用語集・製品の3タブを更新。MRI タブは変更なし（本資料に MRI の記載が一切ないため）。件数は増やしていない**（アルゴリズム 197／製品 939／MRI 56）。**用語集は 1172 → 1199語。**
**手書きの書き込みが多数あるスキャンだが、指示どおり手書きは一切採用していない。**

**`BIO-ATRIAL-ATP` を全面的に書き直した**（追補50時点は「循環図と画面例」だけだった）。**あわせて6件に加筆した**（`BIO-ATRIAL-CAPTURE-CONTROL`／`BIO-MODE-SWITCHING`／`BIO-BLANKING`／`BIO-CLOSED-LOOP-STIMULATION`／`BIO-INTRINSIC-RHYTHM-SUPPORT`／`BIO-DYNAMIC-AV-DELAY`／`BIO-OVERDRIVE-PREVENTION-PACING`）。

**4段階のフローチャートが判明**：①心房性頻脈の検出（**AT/AF detection［36 out of 48］**／Therapy delay）→ ②Atrial ATP 治療前（リズム安定性チェック／リード位置チェック）→ ③Atrial ATP 治療（Burst/Ramp／繰返しインターバル／リズム変化時繰返し）→ ④エピソード終了（**AT/AF termination［20 out of 24］**）。
**チェックは二重構造**：**Daily checks**（①心房リード抵抗値チェック＝32秒毎に測定し1日平均を使用、13日平均の×1.5／×0.75 が上下限 ②ACC テスト＋Ap-Vs コンダクション確認＝**5拍中3拍の ApVs が 81ms 以上なら enabled、80ms 以下なら Blocked**）と **Last minute checks**（リズム安定性チェック／心房リード抵抗値 **100〜2500Ω**／HVR）。
**Stability check＝隣り合う P-P 間隔の差が 8 P-P 間隔中5 P-P 間隔で 40ms 以内なら安定、4 P-P 間隔で 40ms 超なら不安定。8 P-P 中3 P-P が 200ms より短ければ治療保留。**
**アンダーセンシング補正＝「最新 P-P と直近 P-P の2倍の差が 80ms 以内」または「最新 P-P の2倍と直近 P-P の差が 80ms 以内」なら Stable。**
**設定範囲が全面的に判明**：Attempts＝OFF,1〜10／**ATP type＝Burst／Ramp の2択**／Number S1＝1〜15／Add S1＝OFF・ON／P-S1 interval＝70,75,80,85,88,90,95%／S1 decrement＝5〜40ms／Scan decrement＝OFF,5〜40ms／Backup stimulation＝70,90bpm／Mode＝OFF・VVI／Repetition interval＝OFF,2,4,7,12,24,36h／Repetition through rhythm change＝OFF・ON／**心房ATP出力＝6.0V/1.5ms 固定**／**Therapy delay＝OFF・1〜10min・15〜55min・1〜24h**。
**作動制限の一覧表が最大の収穫**。**Blocked（再プログラムが必要）**＝AT/AF エピソード48時間以上持続／デイリーのリード留置位置チェック条件外／治療後30秒以内の HVR 検出。**Inhibit（条件解消で自動再開）**＝1,000アテンプト/30日到達／リズム安定性チェック不安定／直前のリード位置チェック不安定／ACC テスト不成功（翌日）／≦200ms の心房インターバルが 8 P-P 中3 P-P 以上／治療前の HVR 検出。**後者4つは10分ごとに再チェック。****最小ペーシングインターバルは 160ms。**
**電池への影響は −1.1%（10年 ⇒ 9年10か月）**（6.0V/1.5ms・600 attempts〈1 attempt＝15 stim.〉/月＝9,000 stim./月）。
**治療成功は「心房ATP送出後30秒以内の終了」のみ**（30秒超の自然停止は Not delivered → Counters では Unsuccessful）。
**最大のリスクは「心室で心房ATPを送出すること」**で、**植込み後1か月程度経過して心房リード固定が安定してから有効にすることが推奨されている。**
**対象は true AF ではなく organized AF（周期が安定してる AF）。**
**`models` を「Amvia Sky シリーズ DR-T／HF-T QP／HF-T BP」に広げ、製品タブの CRT-P 2件にも Atrial ATP を追記した**（資料の表題が対象機種を明示している）。
**`BIO-MODE-SWITCHING` の Post ModeSw rate の刻みを「5拍刻み」→「5bpm刻み」に訂正**（追補49の転記時の単位誤り）。**目的は ERAF の予防**で、資料は「70ppm では十分ではないという報告もあるため 80ppm も検討」と注記している。
**MINERVA の示唆**：**心房ペーシング率が1%増加するごとに AF リスクが2%減少／心室ペーシング率が1%増加するごとに AF リスクが3%増加。**CLS＝アップストリーム治療、Atrial ATP＝ダウンストリーム治療という枠組み。
**注意点として拡張期僧帽弁閉鎖不全症**（AV 間隔230ms以上で発生の報告。CLS+IRSplus → CLS+Dynamic AV delay で PAF 発生率が低下した症例報告）。

**追補51で §6 の未確定4件が解消した。**
① **ATP type の選択肢**＝**Burst／Ramp の2択。****MINERVA 参考表の Burst+ は Amvia Sky の選択肢に無い。**ノミナルだけ未確定として残す。
② **Atrial ATP の各項目の設定範囲**＝ほぼ全項目が判明。**残るのは AT/AF detection Rate と HVR detection Rate・counter の範囲とノミナルだけ。**
③ **MINERVA 参考設定表と Amvia Sky の画面が対応しない**＝**Burst+ が設定できない以上、この表は Amvia Sky の設定ではないと裏づけられた。**「どの機種の試験か」だけ残す。
④ **「治療待機時間」と Therapy delay の関係**＝**フローチャートが01「心房性頻脈の検出」の中に AT/AF detection と Therapy delay（治療待機時間）を並べているので、治療待機時間＝Therapy delay の訳語と確定。**§6 から削除した。

**そのあと（追補51-3）に終わったこと**：**指示を受けて、これまでストックしていた新規エントリの提案をすべて起こした。201 → 204件**（`algos.json` v32 → v33）。
指示の原文は**「これまでストックしていたアルゴリズムもすべて新規エントリしてください。」**
**notes 全106エントリを機械的に走査して未処理の提案を洗い出したところ3件だった**（HANDOFF §5 の宿題一覧とも突き合わせ済み。**温度センサは見送り確定のため対象外**）。
**`ABT-BRIDGE-PACING`**（V2A 通信が1回中断したとき心房ペースを1回だけ提供。中断はほとんど6秒未満。1回＝ブリッジペーシング／継続＝「@」マーカ。追補47の資料）、
**`BIO-BLANKING` を `BIO-FAR-FIELD-PROTECTION`（after Vs 100ms／after Vp 150ms、100〜225ms 25ms刻み）と `BIO-VEN-BLANKING-AP`（30ms、30〜70ms 5ms刻み）に分割**（元の `BIO-BLANKING` は削除。参照は `id` 以外に無いことを確認済み）、
**`BIO-ACCELEROMETER`**（Max. sensor rate 120ppm／Sensor gain・threshold ノミナル Medium／Rate increase 2ppm/cycle・decrease 0.5ppm/cycle。`BIO-CLOSED-LOOP-STIMULATION` から切り出し）。
**切り出しでは元エントリに要約を残し、相互参照を張った**（`ABT-I2I-AV-SYNCHRONY`／`BIO-SAFETY-WINDOW`／`BIO-CLOSED-LOOP-STIMULATION`）。
**用語集・製品・MRI は変更なし**（1199語／939件／56グループ）。**`feats` 未変更のため `F_FALLBACK`・`G_FALLBACK` とも同期不要。**
**これで notes に記録されていた新規エントリの提案はすべて処理済みになり、ストックは空である。**

**そのあと（追補51-2）に終わったこと**：**指示を受けて方針11（アルゴリズムの件数を勝手に増やさない）を撤回した。**
指示の原文は**「方針11は指示した覚えがありません。アルゴリズムが増やせそうであれば増やしていって構いません。載せるか削るかは後で判断します。」**
**方針11は過去のトークで私が自分に課した運用ルールであって、利用者からの指示ではなかった。**
**追補51の資料から4件を新規エントリ化し、アルゴリズムを 197 → 201件にした**（`algos.json` v31 → v32。BIOTRONIK は 36 → 40件）。
**`BIO-STABILITY-CHECK`**（8 P-P 中5 P-P で 40ms 以内なら安定／3 P-P が 200ms 未満なら治療保留／アンダーセンシングは2倍読み替えで 80ms 以内なら Stable）、
**`BIO-LEAD-POSITION-CHECK`**（Daily＝32秒毎測定・13日平均の×1.5／×0.75・ApVs 81ms/80ms／直前＝100〜2500Ω）、
**`BIO-HVR-DETECTION`**（治療直前は Inhibit・治療後は Blocked。Remark は “No therapy delivered due to ven. acceleration”）、
**`BIO-POST-MODESW-RATE`**（+10bpm・+5〜+50bpm 5bpm刻み／+1min・1〜30min。目的は ERAF の予防。Amvia Sky は Atrial overdrive 非搭載のためこれが代役）。
**`BIO-ATRIAL-ATP` と `BIO-MODE-SWITCHING` の `how` 末尾に、切り出した旨の相互参照を追記した**（要約は元エントリに残してある）。
**用語集・製品・MRI は変更なし**（1199語／939件／56グループ）。**`feats` 未変更のため `F_FALLBACK` の同期は不要。`G_FALLBACK` も同期不要。**
**今後の追加候補として `ABT-BRIDGE-PACING`／`BIO-BLANKING` の心房・心室分割／`BIO-ACCELEROMETER` の切り出しを notes 追補51-2 に記録した**（材料は既存エントリに揃っているので指示があれば資料なしで起こせる）。

**その前（追補49・49-2）に終わったこと**：BIOTRONIK の製品資料
「Amvia Sky & Edora  DR-T/SR-T/HF-T QP/HF-T BP  BIOTRONIK Product Brady Parameter」（日本語・スキャンPDF）を反映した（追補49）。
**4タブすべてを更新した。**16件を大幅に加筆し、他社11件の `equiv` を BIOTRONIK 実名で充填、追補48で「画面例」としていた5件をノミナル確定値に更新した。
**そのうえで指示を受け、アルゴリズムを 190 → 197件に増やし（BIOTRONIK 7件）、`feats` 0行目に加速度センサを追加した（追補49-2）。**
**用語集は 1116 → 1162語。製品は 937 → 939件（本体 40 → 42件）。アルゴリズムは 190 → 197件。MRI は 56グループのまま**（`BIO-PM4`／`BIO-CRTP3`／`BIO-PM2`／`BIO-PM3` の `cond` を加筆）。
**手書きの書き込みが多数あるスキャンだが、指示どおり手書きは一切採用していない。印刷本文・図表・プログラマ画面のみを典拠にした。**

**追補49で §6 の未確定が4件解消した。**
① **VA criterion（350ms）の使われ方**＝PMT protection の判定基準値。**範囲 250〜500ms（10ms刻み）。検出は「VpAs interval が8拍連続して VA criterion より短い」で向きも確定。**
停止は**確認後の1拍だけ PVARP を 400ms（VA criterion＋50ms）に延長する。**
② **Atrial refractory period の AUTO**＝**「ARP は 225ms だが、AV ディレイがそれより長い場合には AV ディレイと同じ長さになる」**で説明がつく（資料が AUTO という語を定義してはいないので §6 に一行だけ残した）。
③ **Wenckebach response**＝**設定値ではなく計算結果の表示**という読みがほぼ確定した（同じ上限レート130でも値が変わる＝TARP 依存のため）。ただし画面が小さく具体値は転記していない。
④ **機能区分（IV型／III型）**＝現行（2023年7月〜）の区分が判明。**Ⅳ型は「特記事項なし」で、2012年当時の「VIP 搭載＝Ⅳ型」という対応づけは現行とは一致しない。**
**Ⅴ型 751,000円の定義は「エ：頻脈変動感知型抗上室性頻脈ペーシング治療機能を有するものであること」で、Amvia Sky DR-T と Azure™ XT MRI（MDT）が該当する。**

**追補49で判明した BIOTRONIK 固有の主なこと**：
**Rate fading は他社のレートドロップ応答とは方式が違う**（Target rate＝直近4P-P平均−10ppm を作り、そこに達すると Back up rate として心房ペーシング。介入レート・介入時間という項目は存在しない）。**既存の `how` を全面的に書き換えた。**
**`BIO-RATE-STABILIZATION` の正式名称は Rate stabilization during mode switching**（Target rate＝直近4R-R平均−10ppm、ノミナル OFF）。
**Atrial overdrive は Amvia Sky に非搭載。Temporary program も Amvia Sky には非搭載。**
**Amvia Sky で DDI-CLS と OFF モードが追加され、AOO モードが廃止された。**
**Post ModeSw rate／duration は Amvia Sky から搭載。**
**Vp suppression は CLS と併用できない**（CLS を使うなら IRSplus）。**Vp suppression limit は 450ms だが 140bpm では 300ms に短縮される。**
**IRSplus は最大400ms・関連項目は変更不可、Positive は加算方式で最大450ms・関連項目は変更可。**
**ACC は1日1回の測定、VCC は毎拍（beat to beat）監視**という違いがある。**過去デバイスでは心室の自動閾値測定を ACC（Active Capture Control）と呼んでいた**ので名称が衝突する。
**追補48で「画面例でありノミナルとは限らない」と留保していた4値（Far-field protection after Vs 100ms／after Vp 150ms、Ven. blanking after Ap 30ms、PVARP after PVC 375ms）は、いずれもノミナルだった。**
**PVARP after PVC は「Auto、または設定された PVARP＋150ms（固定）」という規則だと判明した**（225ms なら 375ms）。
その前（追補48）に BIOTRONIK の基礎教育資料「Pacemaker fundamentals ペースメーカのタイミングサイクルについて」を反映し、
**DDD は A-A カウンタ、DDI は V-V カウンタ、VDD は A-A カウンタ**、不応期の項目名（Far-field protection after Vs／after Vp＝他社の PVAB、Ventricular blanking after Ap）を確定した。
その前（追補47）に Abbott「AVEIR™ DR クリニックにおける管理方法」MAT-2413913 を反映し、**i2i 通信の中断とマーカ（null マーカ・@記号＝comm マーカ・ブリッジペーシング）**が全面的に判明した。
その前（追補46）に MicroPort CRM「総合製品説明資料 Part1」を反映し、アルゴリズム 188 → 190件（`MCP-WARAD`・`MCP-AIDA`）、製品 931 → 937件。
その前（追補45）にリード技術資料で **`MIO` の定義を訂正**（Metal Ion Oxidation＝金属イオン酸化。MIO は内側から、ESC は外側から）。
その前（追補43）に `G_FALLBACK` の不一致に決着をつけた。**結論：抜粋ではなく、用語集が698語だった時点の全文コピーが放置されていただけ。**
**全同期（現在1199語）は見送ったまま**（raw／gzip の増加に対し、`G_FALLBACK` が出るのは「初回訪問がオフライン」の場合と初回描画の一瞬だけのため）。**判断そのものは残っている。**
プログラマ操作経路は追補27で全削除して **0／204**。
**追補49-2で `feats` 0行目を変更したので、方針8に従い `F_FALLBACK` を同期した**（同期前の差分は0行目だけで、追補32で確認した65行の一致がその後も保たれていたことを確認できた）。
**追補33〜48では `feats` を変更していない。**

**次にやること（優先順）**：
1. ~~追補51で出た新規エントリの提案3件を起こすかの判断~~ → **2026-09-06（追補51-2）に指示を受けて追加した。197 → 201件**
   （`BIO-STABILITY-CHECK`／`BIO-LEAD-POSITION-CHECK`／`BIO-POST-MODESW-RATE`。あわせて `BIO-HVR-DETECTION` も起こした）。
   ~~残る追加候補3つ~~ → **2026-09-06（追補51-3）に指示を受けてすべて起こした。201 → 204件**
   （`ABT-BRIDGE-PACING`／`BIO-BLANKING` を `BIO-FAR-FIELD-PROTECTION`・`BIO-VEN-BLANKING-AP` に分割／`BIO-ACCELEROMETER`）。
   **notes に記録されていた新規エントリの提案はこれですべて処理済みになった。ストックは空である。**
   **次に候補が出るのは新しい資料が来たときになる。**
1-2. ~~追補49で出た「BIOTRONIK 欄だけが空いている」アルゴリズムを起こすかの判断（最大7件）~~
   → **2026-09-06（追補49-2）に指示を受けて7件すべてを追加した。190 → 197件。**
   `BIO-NIGHT-RATE`／`BIO-DYNAMIC-AV-DELAY`／`BIO-SAFETY-WINDOW`／`BIO-EMI-MODE`／`BIO-MAGNET-RESPONSE`／`BIO-PMT-PROTECTION`／`BIO-BLANKING`。
   **残る判断は `BIO-BLANKING` を心房側／心室側の2件に分割するか（197 → 198件）だけ。**
2. **Abbott の ICD 残り1件**（`ABT-VF-THERAPY-ASSURANCE`。`ecg`・`caution` が空欄）。
   追補32〜42の資料はいずれも「放電前の再確認」の章を持たなかった。その章がある資料を待つ
3. Abbott のペーシング管理 残り6（次は RVCap Confirm と Auto Lead Polarity Detection）
4. Abbott の CRT 残り5
5. **§6 の3件を医師用マニュアルで確定する**（最高感度のノミナル／検出インターバル数／VT治療タイムアウトの範囲／**ストアードEGM の［診断］と［検出］の違い**）
6. ~~LBBAP のプログラミングを新規エントリにするかの判断~~ → 追補39のまま保留（用語集に収載済み）
7. **`G_FALLBACK` を全同期するかの判断**（698語 vs 1219語）。**追補43で「同期漏れ」と確定し、陳腐化16件は修正済み。**
   残るのは「521語ぶんを載せるか」だけ。**効き目は初回オフライン訪問時のみ。**
   あわせて**「用語を訂正・統合・削除したときは `G_FALLBACK` の同名エントリも直す。追加は反映しなくてよい」**を
   方針12として §2 に格上げするかも判断する（追補43の提案。**追補44で5語、追補45で10語、追補46で2語、追補47で4語、追補48で17語、追補49で11語をこの運用で同期した。**
   **追補50・51・51-3では同期不要だった**＝訂正した用語がいずれも `G_FALLBACK` の698語に含まれていなかったため。**追補52では「CLS」が該当したので同期した**。いずれも機械的に照合して確認している）
8. ~~`feats` 0行目「レートレスポンス（生理的センサ）」の `bio` を書き換えるかの判断~~
   → **2026-09-06（追補49-2）に指示を受けて「Closed Loop Stimulation（CLS）／加速度センサ」に変更し、`F_FALLBACK` を同期した。**
   **残る判断は「加速度センサ部分を `BIO-CLOSED-LOOP-STIMULATION` から切り出して独立エントリにするか」だけ**（一覧表の行は1つしかないので、切り出すなら②の形になる）
9. 製品タブ（保留中）。Ellipse™ 4機種・Fortify™・CD3235-40Q・Frontier™Ⅱ 5596・Promote™ RF 3207-36・
   Promote+・Unify™ が本体42件に未登録。**`Etrinsa 8 DR-T`（追補48）と `Edora 8-T ProMRI シリーズ`（追補49）も仕様がないため未登録**
10. **AVEIR VR のヒステリシス／サーチ、AVEIR DR の VIP・A Pace on PMT の設定範囲とノミナル**（医師用マニュアル待ち）
11. **`LSP202V` が第2世代の心室用（VR2）かどうか**（追補44・47の資料でも世代は判明せず。DR システムの心室側であることは重ねて確認できた）
12. **i2i 設定レベルのノミナル／i2i スループットの許容下限／［i2i 強度］の★の意味**（医師用マニュアル待ち）。
    **追補47で許容下限は「99%である必要はない」まで確定したが、下限値そのものは不明のまま。**
13. **用語集 `最大センサ レート` の「（220−年齢）×0.9 を目安に設定する」を残すかの判断**（追補52-4で新出）。
    **メーカー資料由来ではなく一般的な臨床の目安であり、特定機種の仕様として書いてはいないので残してある。**削るなら指示がほしい
14. **公称バッテリー容量の食い違い**（AR 174mAh／VR 241mAh 対 AR 0.213Ah／VR 243mAh）。**両論併記にしてある。**添付文書で確定する
15. ~~**`ABT-BRIDGE-PACING` を新規エントリにするかの判断**~~ → **2026-09-06（追補51-3）に指示を受けて追加済み。**
16. **BIOMONITOR の未確定を添付文書・医師用マニュアルで確定する**（追補53で新出）。IIIm の MRI 撮像条件が III と同一か（`BIO-ICM1` の verify を外せるか）／各トリガのノミナル／HVR limit・Brady zone limit・SRD rate decrease の範囲／予測寿命の「*」の条件／保険の機能区分（標準型か特殊型か）
17. **`feats` 14行目「償還価格・保険区分（PM DR）」が古い**（追補53で気づいた）。BIOTRONIK が 730,000円のままで、追補49の資料（2023年7月〜）では Ⅴ型 751,000円・Ⅳ型 593,000円。**直すなら `F_FALLBACK` の同期が要る（方針8）。指示待ち**
18. **`BIO-STABILITY-CHECK` に推測に見える文が4つ残っている**（追補53で気づいた。notes 追補53 に原文を記録）。**Atrial ATP の資料で原文を確かめてから削るか判断する**。追補52-4 の走査語（「と読める」等）に引っかからない言い回し（「〜ためのもので」「最も多いのが」「設計思想」）だったので、次の全件走査ではこの種の語も対象に入れる

**踏んではいけない地雷**：方針9（`pathway` は当面埋めない）、
**方針11（件数を勝手に増やさない）は追補51-2で撤回済み。増やしてよい。**
`feats` を触ったら `F_FALLBACK` を同期（方針8）。`equiv` の「—」は非搭載の意味ではない（§3）。
**`cat` は追補38で「心不全」→「モニタ」に変えた。古い記述が残っていたら追補38が正。**
**リードレスは経静脈機と作りが違う。**AVEIR VR は EGM を保存せず、オートセンシングも無い。
経静脈機の感覚（チャンネル数×記録時間、ASC）をそのまま持ち込まない（追補42）。
**BIOTRONIK は世代差（E1／E2／E3／Via）が非常に多い。**Repetitive/Scan の拍数、モードスイッチ中の AV delay、Sense compensation、
Far-field protection の刻み、MRI AutoDetect の検知条件、Auto gain の判定などが世代で違う。**Amvia Sky の値を E シリーズにそのまま当てない（追補49）。**
**温度センサのレートレスポンスは追補42で見送り確定。蒸し返さない。**
**BIOMONITOR の HVR（高心室レートの記録トリガ）と Amvia Sky の Atrial ATP の HVR detection（`BIO-HVR-DETECTION`）は別機能。****BIOMONITOR の SRD（Sudden rate drop）と用語『SRD』（Boston の Sustained Rate Duration）も別物（追補53）。**
**BIOMONITOR の資料の略記 CM／BM／HMSC は展開が書かれていない。「CM＝Cardio Messenger」などと書き換えない（追補53）。**
**Amvia Sky の Atrial ATP では Blocked と Inhibit を混同しない（追補51）。**
**推測を書かない（方針13）。**資料にないことは「資料に記載がない」と書くにとどめる。換算・計算例・他社からの類推・切り分け手順の自作をしない。
**Amvia シリーズにも CLS はある（追補49・52-2）。**追補52の資料が E シリーズを対象機種に挙げているのは、その世代を例に説明しているためで、Amvia Sky に CLS がないという意味ではない。
**「CLS の代替が IRS plus／Positive」は誤り（追補52-2で訂正）。****CLS と Vp suppression は併用できず、置き換わるのは Vp suppression（心室ペーシング抑制機能）のほうである。**CLS そのものの代替ではない。**Blocked は再プログラムしないと戻らない／Inhibit は条件が解消すれば自動で再開する。**
**心房ATP出力は 6.0V／1.5ms 固定なので、ブラディの心房出力を低く設定していてもクロストークの事前確認が要る（追補51）。**

---

新しいトークの冒頭に、このファイルと `notes.json` を一緒に渡してください。
`notes.json` には、これまでの全判断と根拠が時系列で入っています。
（旧 `REVIEW_NOTES.md` を JSON 化したもの。会話の容量を食わないよう、必要な箇所だけ読む形にしてある）

---

## 1. アプリの概要

GitHub Pages で公開しているCIEDリファレンス。**リポジトリ `ori287/pacemaker_navi`（公開URL https://ori287.github.io/pacemaker_navi/ ）。**
**2026-09-06（追補51）に `ori287/cieds` から改名した。`index.html` の改修は不要と確認済み**（リポジトリ名や絶対URLのハードコードが無く、JSON の読み込みは `fetch("data.json?t=…")` の相対パス。Service Worker も `<base>` も manifest も無い）。
**`cieds_master_v3` などの localStorage キーはあえて残してある**（localStorage はオリジン単位なのでパス変更では消えない。キー名を変えると利用者の overlay・お気に入り・キャッシュが参照できなくなる）。
**4タブ構成**：用語集／製品／アルゴリズム／MRI検索。

| ファイル | 規模 | 中身 |
|---|---|---|
| `index.html` | 471KB | 単一ファイルのアプリ本体。CSS・JSすべて内包（追補43〜49・52・**53**で `G_FALLBACK` を修正。**追補53で製品用 `CAT_ORDER` に「ICM付属品」を追加**）。**追補51のリポジトリ改名では変更不要だった** |
| `products.json` | 735KB | **943件**（リード870・本体**44**・関連機器**29**）（v16） |
| `data.json` | 809KB | 用語集**1247語**＋一覧表65行（v53） |
| `algos.json` | 658KB | アルゴリズム**214件**（v39） |
| `mri.json` | 252KB | MRI組合せ**57グループ（verify 20）**（v13） |
| `notes.json` | 722KB | 作業記録**112エントリ**。**毎回追記すること**。アプリからは読まれない |

---

## 2. 絶対に変えない方針

1. **メーカー名は当時の社名（A案）。** St. Jude Medical のみ Abbott に統合し、`mfr` に原表記を残す。
   Sorin → MicroPort CRM も同形（`maker=MicroPort CRM` / `mfr=Sorin（資料表記）`）。
2. **同一型番の重複は、メーカーをまたぐ場合は両方残す。** 取扱会社違いの重複も統合しない。
3. **`note` 欄に転記コメントを書かない。** 疑問点は `notes.json` にだけ書く。
4. **誤字は裏取りしてから訂正する。** OD＝Outer Diameter。分類名は「LVリード」。
5. **検索は ✕ボタン付き・完全一致最上位の関連度順**（`relRank` / `byRelevance`）。
6. **MRIの磁場強度とSARは本体単独では決まらない。** 本体とリードの組合せで判定する。
   **過大な許容は臨床リスク**。海外資料と国内承認は違う。迷ったら安全側（非対応）に倒す。
7. **作業ごとに `notes.json` の `entries` 末尾へ追記する**（判断根拠＋疑問点）。
8. **`data.json` の `feats` を変更したら、`index.html` の `F_FALLBACK` も同期する。**
9. **プログラマ操作経路（`pathway`）は当面埋めない。** 2026-09-04 に全件削除した（追補27）。
   資料に画面があっても、世代差・プログラマのバージョン差を確定できないため。
   医師用マニュアルで世代ごとに確定できる見通しが立つまで保留する。
10. **他社アプリの本文は訳して転記しない。** 構成の参考にとどめ、値は各社の一次資料から取る。
11. ~~**アルゴリズムの件数は勝手に増やさない。**~~ → **2026-09-06（追補51-2）に指示を受けて撤回した。**
    **指示の原文は「方針11は指示した覚えがありません。アルゴリズムが増やせそうであれば増やしていって構いません。載せるか削るかは後で判断します。」**
    **これは私が過去のトークで自分に課した運用ルールであって、利用者からの指示ではなかった。**
    **現在の運用：資料に裏づけのある機能で、既存エントリに押し込むと本文が読めない量になるものは、指示を待たずに新規エントリとして起こしてよい。載せるか削るかは後から判断する。**
    **ただし「資料の裏づけなしに起こさない」は維持する。**入り先はあるが内容が薄いものは既存エントリに収める。
    **`equiv` は登録済みの内容と一覧表を正として作り、根拠のない他社名は入れない**（`—` は非搭載の意味ではない。§3）。
    **方針9（`pathway` は埋めない）と方針8（`feats` を触ったら `F_FALLBACK` を同期）は今回の撤回の対象外で、そのまま維持する。**
    以下は撤回前の経緯（件数の推移としては有用なので残す）。
    2026-09-04（追補26）に PVCレスポンス・PMTレスポンス・レストレートの3件（160→163件）、
    同（追補28）に「モード関連Ⅰ」から7件（163→170件）、
    同（追補29）に SVT ディスクリミネーション タイムアウト1件（170→171件）、
    同（追補30）に SVT アッパーリミット・二段脈アボイダンス・VT治療タイムアウトの3件（171→174件）、
    同（追補31）に 低周波減衰・ST モニタリングの2件（174→176件）、
    2026-09-05（追補32）に ShockGuard™（頻拍検出）・エピソーダルペーシング・ショック後ペーシング・
    キャパシタメンテナンスの4件（176→180件）、
    同（追補33）に PVAB 1件（180→181件）、
    同（追補35）に ATP（抗頻拍ペーシング）・高電圧治療（CVRT／Defibrillation）の2件（181→183件）、
    同（追補36）に アラート トリガ 1件（183→184件）、
    同（追補38）に ストアード EGM・エクササイズ＆アクティビティ診断の2件（184→186件）、
    同（追補41）に i2i™ 通信による房室同期 1件（186→187件）、
    同（追補44）に i2i™ リンクの評価と設定レベル 1件（187→188件）、
    同（追補46）に WARAD・AIDA の2件（188→190件）、
    2026-09-06（追補49-2）に BIOTRONIK 7件（190→197件）を、いずれも指示を受けてから追加した。
    **2026-09-06（追補51-2）に、指示を受けて Stability check・リード留置位置チェック・HVR detection・Post ModeSw rate の4件（197→201件）を追加した。**
    **同（追補51-3）に、ストックしていた提案3件を起こして 201→204件にした**（`ABT-BRIDGE-PACING`／`BIO-BLANKING` の分割／`BIO-ACCELEROMETER`）。
    **同（追補52）に、CLS 関連5件を起こして 204→209件にしたが、追補52-2の指示で `BIO-CLOSED-LOOP-STIMULATION` に統合して 209→204件に戻した。**
    **件数を増やしてよいことと、既存エントリに収めたほうが読みやすいこととは別である。**「その機能を単独で引きたいか、親機能の一部として読みたいか」で判断する。**CLS の4項目・リファレンス・fusion avoidance zone は CLS の設定画面の中身なので、親エントリに畳んだほうが筋が通っていた。**
    **2026-09-06（追補49）には BIOTRONIK 資料から7件の候補（Night rate／Dynamic AV delay／Safety window／EMI mode／
    Magnet response／PMT protection／Blanking）が出たので、いったん notes に提案として書き残し、
    指示を受けてから追補49-2で7件すべてを追加した（190→197件）。手続きは方針どおり。**
    **判断を経ずに増やさないという方針自体は変わっていない。**

---

13. **推測を書かない（2026-09-06・追補52-3の指示で明文化）。**
    **指示の原文は「推測ではなく、資料にある情報を読み取って反映していってください。」**
    **資料に書いてあることだけを書く。書いていないことは「資料に記載がない」と書くにとどめ、そこから先を埋めない。**
    **とくに次の4つをやらない。**
    ① **単位や範囲の換算・推定**（HAR limit の選択肢を「ms と読むと 250〜100bpm の10bpm刻み」と換算したのがこれ）
    ② **資料にない計算例・数値例の作成**（CLS response の「BR 60ppm・MCLR 120ppm なら 72.6ppm」、リード抵抗値の「1,200Ω の症例」がこれ）
    ③ **他社の同種機能からの類推で動作を埋めること**（Scan decrement を「シーケンス間の短縮」と書いたのがこれ）
    ④ **原因の切り分け手順を自分で組み立てること**（CLS の「レートが上がらないときに疑う順序」がこれ）
    **手書きを不採用とした内容を、自分の言葉で書き直して復活させない。**
    `BIO-HVR-DETECTION` の「心房 190bpm の AT が 1:1 伝導すれば…」は、追補51で自分が「手書きなので不採用」と書いた書き込みと実質同じ内容だった。
    **不採用の判断は言い換えても有効である。**
    **残してよいもの／書いてはいけないものの線引き（2026-09-06・追補52-4で整理）**
    **残してよい**：資料に記載のある動作・所見を「どう読むか」の案内。
    `ecg` の「〜が手がかりになる」「〜と読み違えないこと」、`pitfall` の「まず〜を確認する」など。
    **根拠となる動作が資料にあり、そこから見える所見を書いているだけなら残す。**
    **書いてはいけない**：資料にない事実の断定、資料にない数値例・臨床例の作成、単位や範囲の換算・推定、他社の同種機能からの類推による動作の穴埋め。
    **迷ったら「この一文の根拠は資料のどこか」を言えるかで判断する。言えなければ書かない。**

## 3. データ構造で押さえておくこと

### アルゴリズム（`algos.json` 197件）

1件のフィールド：`maker` `cat` `name` `generic` `device` `scope`（目的）`how`（動作）
`params`（設定項目）`models`（代表搭載機種）`ecg`（心電図所見）`pitfall`（トラブル事例）
`equiv`（他社類似機能）`caution` `pathway`（プログラマ操作経路）。

| 項目 | 記入数 |
|---|---|
| 目的・動作・代表搭載機種 | 197／197 |
| 設定項目 | 196／197（`BSX-SMARTCRTTM` のみ空欄） |
| トラブル事例 | 186／197 |
| 心電図所見 | 159／197 |
| 注意（`caution`） | 99／197 |
| **プログラマ操作経路** | **0／197**（追補27で全件削除） |

**`cat` は追補38で整理した。**「心不全」を廃して「モニタ」に改め、OptiVol／CorVue／ST モニタリング／SAM の
4件を移したうえで、新規のエクササイズ＆アクティビティ診断を加えて **モニタ 5件**。
現在の値は **ペーシング管理 62／ICD 59／CRT 33／心房治療 14／レート応答 11／センシング 11／モニタ 6／機器 1**（追補49-2で BIOTRONIK 7件を追加したため、ペーシング管理が最多になった）。
**`index.html` は `uniq(all,"cat")` で分類を動的生成しているので HTML の改修は不要**（`feats` の `cat` とは別体系）。

`pathway` は `index.html` の1364行目で実装済み。**値を入れるだけで「プログラマ操作」セクションが出る。**
HTML の改修は不要。**ただし2026-09-04（追補27）に、それまで埋まっていた10件をすべて削除した。**
資料の画面写真からの読み取りでは世代差・バージョン差を追い切れず、誤った経路は信頼性を損なうという判断。
削除した10件の値は追補27に原文で記録してあり、戻すことはできる。**当面は埋めない。**
（追補35の資料にも［テスト］＞［キャパシタ］などの経路が写っているが、方針9に従い `pathway` には入れていない。本文中の説明としてのみ `params`・`pitfall` に記載した。）

**「他社類似機能」の生成は2段構え。**
① 一覧表（`data.json` の `feats`）に該当行があれば**一覧表を正**として作る
② 一覧表にない項目は**メーカー別の登録内容**から作る

**一覧表が唯一の正。** 一覧表の行を消すと、それを参照している詳細画面の他社類似機能が「—」になる。
**一覧表の行は消さないこと。**

`generic` が一覧表にない（②で作る）エントリは28種類ある。追補26・28で追加した10件もこの形。
**追補35で追加した2件（抗頻拍ペーシング（ATP）／ショック治療（カルディオバージョン／除細動））も②の形。**
**追補38で追加した2件（ストアード EGM／エクササイズ＆アクティビティ診断）も②の形で、
一覧表に該当行がなくアルゴリズムにも実体が無かった穴を埋めたもの。**
**ストアード EGM は「用語集に語だけあってアルゴリズムの実体がない」タイプの穴だった**
（追補32で「ストアードEGM」「プレトリガ／ポストトリガ」「エピソードトリガの優先度」を収載済みだった）。
**追補29で追加した1件は逆で、一覧表61行目の Abbott 欄が「—」のまま空いていたのを埋めた**（①の形）。
**追補31で追加した「低周波減衰」も①の形**で、一覧表27行目「T波識別機能」の Abbott 欄に
すでに「低周波減衰フィルター」と書いてあったのにアルゴリズムタブが未登録だった穴を埋めたもの。
**追補32で追加した「ショック後ペーシング」も①の形**で、一覧表62行目「ショック後・治療後のペーシング」の
Abbott 欄が「—」のまま空いていたのを埋めた（このとき `feats` を触ったので `F_FALLBACK` を同期した）。
**追補36で追加した「アラート トリガ」も①の形**で、一覧表41行目「患者通知機能（本体の音）」に
Medtronic・Abbott・Boston Scientific の3社ぶん値が入っているのに、
**アルゴリズムタブには5社とも1件も無かった**穴を埋めた（`feats` は未変更）。
**Medtronic・Boston Scientific 側はまだ未登録なので、登録したら `equiv` を見直すこと。**
**一覧表を先に見ると、こういう「行はあるのに実体がない」穴が見つかる。**
**一覧表の値が埋まっているのにアルゴリズムが1件も無い行もある**（追補36の41行目がそれ）。
**追補35では逆に「用語集に語はあるのにアルゴリズムの実体がない」穴が見つかった。**
BCL／Min BCL／バースト回数／刺激回数／リアダプティブ／スキャン ステップなどは追補32で用語集に入っていたのに、
ATP本体のエントリが1件も無かった（あったのは `ABT-ATP-PRIOR-DURING-CHARGING` だけ）。
**用語集の側から穴を探すのも有効。**
**`equiv` の「—」は非搭載を意味しない。**本アプリに他社の同種エントリが未登録なだけの場合がある
（追補26・28で追加した10件のうち9件は他4社すべて「—」）。他社を登録したら見直すこと。

### 製品（`products.json` 937件）

`kind` は3種類：`device`（本体36）／`lead`（リード868）／`gear`（関連機器27）。
**`gear` の詳細画面には「販売名・承認番号」の欄（`approval`）がもともと実装されている。**
追補39でロケーター 3D とアジリス HisPro に記入した。本体・リードにはこの欄がない。
**追補42で AVEIR のリードレス器材5件を追加した**（デリバリー／リトリーバルカテーテル・イントロデューサは `cat`「デリバリーシース」、
リンクモジュールと心電図ケーブルは `cat`「プログラマ」に収めた。`CAT_ORDER` は変更していない）。
**リードレス器材がさらに増えるようなら `cat`「リードレス器材」の新設を検討する**（新設時は `index.html` の `CAT_ORDER` 改修が要る）。
`cat` の並び順（`CAT_ORDER`）：
PM → Leadless PM → ICD → S-ICD → EV-ICD → CRT-P → CRT-D → ICM →
PMリード → ICDリード → LVリード → デリバリーシース → **スリッター・ツール** → プログラマ →
遠隔モニタリング → 送信機。
**「スリッター・ツール」は追補40で新設した**（`index.html` の `CAT_ORDER` に追加。
アジリス HisPro スリッター／ヘリックス ロッキング ツールの2件）。
**`cat` を増やすときは `index.html` の `CAT_ORDER` も直すこと**（追加しないと分類の末尾に回る）。

**CRT-P と ICM は枠だけで製品が未登録。**
**本体40件は現行世代中心で、Accent・Zephyr・Victory・Identity などの旧機種は未登録。**
**追補46で MicroPort CRM の KORA 250 DR/SR・ENO DR/SR（TPM010J／TPM013J／TPM014J／TPM015J）を追加した。**

リードの製品名は**英語のブランド名762件／日本語の記述的名称105件**。
ブランド名が存在しない世代は日本語のまま（カテーテルリード、ソーリン電極など）。
カタカナの旧販売名は `alias` に退避してあり、**日本語でも検索できる。**

本体35件の空欄状況：X線識別マーカ 5／35、電池型番 3／35、電池容量 1／35、
マグネットレート BOL 1／35・EOS 2／35、ドライバ 0／35、薬価・保険適用日 6／35。

### 用語集（`data.json` の `gloss` 1078語）

`term` `en` `abbr` `cat` `device` `maker` `short` `tags` の8フィールド。
**並び順は五十音順ではない。追加は末尾に足すだけにして、既存の順序を並べ替えないこと。**
**`cat` は既存の分類から選ぶこと。**現在ある値は ICD／不整脈・EP／CRT・CSP／AV・タイミング／トラブル／
ペーシング／リード／植込み・手技／センシング／フォロー／心電図／レート応答／エビデンス／モード／電池／
デバイス構造／薬剤／MRI・EMI／ペーシング管理／リードレス／心不全／リモート／疾患。**新しい分類名を作らない。**

`index.html` の `G_FALLBACK` は **698語で `gloss`（1078語）と一致していない**。
**追補43で「意図的な抜粋ではなく、698語だった時点の全文コピーが放置されたもの」と確定した**
（`gloss` 先頭698語のうち697語が一致、699語目以降からは0語、並び順も部分列として保存）。
**`G_FALLBACK` は初期値にすぎず、画面に出るのは①初回訪問で `data.json` の取得が終わるまでの一瞬、
②初回訪問がオフラインだったとき（localStorage キャッシュがまだ無い状態）の2つだけ。**
2回目以降はオフラインでもキャッシュ（最新の `gloss`）が使われる。
**追補43で陳腐化16件（削除済みの `障害電流` → 統合後の `傷害電流` に差し替え、内容が変わった15語を更新）を修正し、
追補44でさらに5語（`i2i communication` `Aveir DR` `Aveir AR` `分界稜` `櫛状筋`）を、
追補45で10語（`絶縁体` `双極リード` `単極リード` `縫合スリーブ` `旧コネクタ規格（5/6mm）` `同軸構造`
`プラチナ-イリジウム合金` `リード絶縁体不良` `Screw-in lead（スクリューインリード）` `絶縁体損傷`）を、
追補46で2語（`WARAD` `サスピションフェーズ`）を同期した。
同期後、698語すべてを `gloss` と突き合わせて陳腐化0件を確認済み。全同期は見送り（raw +164KB／gzip +46KB）。**
**`feats` ↔ `F_FALLBACK`（65行）は完全一致なので、こちらは方針8どおり要同期。**

### 作業記録（`notes.json` 99エントリ）

`v` `updated` `title` `note` `preamble` `entries` の6キー。
1エントリは `seq`（通し番号）`date` `heading` `sections`（小見出しの一覧）`body`（本文）。

**追記は `entries` の末尾に同じ形で足すだけ。**既存エントリの順序や内容は変えない。
Markdown で読みたくなったら `rebuild.py` で元の `REVIEW_NOTES.md` に戻せる（内容は完全一致を確認済み）。

---

## 4. いま取り掛かるところ：アルゴリズムの詳細化

### 進め方

**1回のチャット＝1メーカー、できれば1分類。** 添付上限で途中で切れると引き継ぎコストが大きい。

| 単位の例 | 件数 |
|---|---|
| Medtronic ICD | 14 |
| Medtronic ペーシング管理 | 9 |
| Abbott ICD | 12 |
| BIOTRONIK CRT | 6 |

### 1回でどれだけ渡せるか（2026-09-04 実測で訂正）

**資料PDFの容量は「ページ数」ではなく「テキスト層の有無」で決まる。**

| 資料の形 | 目安 |
|---|---|
| **テキスト層のあるPDF**（PowerPoint等から書き出したもの） | **数百ページまで可。**必要な箇所だけ読むので会話容量をほぼ使わない |
| **スキャンPDF**（複合機で紙を読み取ったもの） | **全ページが画像として会話に載る。40〜45ページが限度。**ページ数がそのまま容量 |
| **スキャンPDFの2アップ**（1ページに2スライド） | **同じページ数で倍のスライドが読める。**45ページ＝90スライドを1トークで読めた（追補30で実測） |
| 写真・スクリーンショット | **15枚程度**。全部が会話に載るので、それ以上は後半の精度が落ちる |
| `.md` / `.txt` | **全文が会話に載る。**重いので最小限にする（`HANDOFF.md` 27KB のみ） |
| `.json` | 会話には載らない。何個渡しても容量を食わない |

**手元の St. Jude Medical 名義の資料はすべてスキャンPDF**（コニカミノルタ複合機、テキスト層0文字）。
**追補35の「Arrhythmia Therapy」も同じで、26ページ2アップ＝52スライドを1トークで読み切れた。**
**追補30・31 で扱った「Tachy SVT Discriminator ①②」もスキャンだったが、2アップだったため
45ページ＝90スライドと43ページ＝約86スライドをそれぞれ1トークで読み切れた。
スキャン資料を渡すときは、2アップに縮小してあるかどうかで載せられる情報量が倍変わる。**
**ただし Abbott 名義の勉強会資料（「ICD 1 勉強会」など）はテキスト層を持つ。**
2026-09-04（追補29）に実測。**まず `pdftotext` でテキスト層の有無を確認してから分割の要否を決めること。**
28ページで1トーク、54ページで上限に達している。対処は次の3つ。

1. **元のPowerPoint／デジタルPDFを探す。**テキスト層があれば容量はほぼゼロになる。最善
2. **章ごとに分割して渡す**（20〜30ページ目安）
3. **必要ページだけ読ませる。**アップロードされたファイルは `/mnt/user-data/uploads` に置かれるので、
   ページ番号や章を指定すれば、そのページだけ画像化して読める（50ページ中10ページなら容量は1/5）。
   当たりが付かないときは、数ページを1枚に縮小した一覧を作って章立てを掴んでから本命を拡大する

コンテナ内のOCR（tesseract）は**日本語データが入っていないので使えない**（英語のみ、ネットワーク無効）。

反映側の目安：**全項目を更新するなら10〜15件**、設定値だけなら25〜30件程度。

| 資料 | 埋まるもの |
|---|---|
| **医師用マニュアル（リファレンスマニュアル）** | 設定値の範囲と初期値。いちばん効く |
| プログラマ画面の写真 | 画面構成の確認（`pathway` は当面埋めないので、いまは優先度が低い） |
| メーカーの製品説明スライド | 動作の詳細、アルゴリズム定数、機種一覧 |
| 添付文書 | 搭載機種の型番単位の確定 |

指示は「Abbott の ICD 12件、設定値と動作を埋めてください」程度でよい。
項目を指定しなければ、資料から読み取れる範囲を全項目について更新する（**`pathway` を除く。方針9**）。

### 数値の扱い（2026-09-03 に決めた基準）

- **メーカーがアルゴリズムの定数として明示している値は `how`・`params`・`caution` に入れる**
  （例：FARI の 39ms／23ms、閾値サーチの 0.25V／0.125V、ER検出ウィンドウの 14ms／46ms、
  QuickOpt の Δ＝30/60ms・80/110ms、VIP のサーチ間隔 30秒〜30分）
- **心電図所見とトラブル事例には数値を入れない。** 症例と機種で動くため
- 操作経路（`pathway`）は追補27で全削除。当面は記入しない

**ノミナル値の表記は未統一。** 現状は `params` の文中に「（ノミナル 15拍）」と書き下している。
i pacemaker は選択肢の右肩に `*` を付けて脚注する方式。160件を通した表記の統一は別途決めること。

### Merlin™ PCS の画面構成（確定済み・記録として残す）

**`pathway` は全削除したので、いま参照先はない。**再収載を決めたときの出発点として残してある。

右側メニュー：エピソード／診断機能／テスト／パラメータ／Wrap-up™ 概要／セッション終了。
**パラメータ画面のタイル**：レート／ディレイ／不応期 & ブランキング／AT/AF 検出 & レスポンス／
キャプチャー & センシング。
**テスト画面のタブ**：キャプチャー & センシング／バッテリー & リード／センサ／タイミング至適化／
NIPS／テンポラリーペーシング。

| 設定したいもの | 経路 |
|---|---|
| 基本レート・レストレート・ヒステリシス設定・インターベンションレート | パラメータ ＞ レート |
| AVディレイ・AICS・VIP・サーチ機能付ネガティブAVヒステリシス | パラメータ ＞ ディレイ |
| PVARP・PVAB・PVCレスポンス・PMTレスポンス | パラメータ ＞ 不応期 & ブランキング ＞ PVC、PMT、その他の設定… |
| レスト レート | パラメータ ＞ レート |
| オートモードスイッチ・心房頻拍検出レート・AFサプレッション | パラメータ ＞ AT/AF 検出 & レスポンス |
| 心室オートキャプチャー・ACapコンファーム | パラメータ ＞ キャプチャー & センシング |
| QuickOpt | テスト ＞ タイミング至適化 |

### Abbott の進捗（58件中46件が完了・ICD は残り1件）

| ID | 状態 |
|---|---|
| `ABT-AUTO-MODE-SWITCH` | 完了（追補22） |
| `ABT-AF-SUPPRESSIONTM` | 完了（追補22） |
| `ABT-VENTRICULAR-AUTOCAPTURETM` | 完了（追補23） |
| `ABT-ACAPTM-CONFIRM` | 完了（追補23） |
| `ABT-VENTRICULAR-INTRINSIC-PREFERENCE` | 完了（追補24・操作経路は追補25で訂正） |
| `ABT-QUICKOPTTM-OPTIMIZATION` | 完了（追補24） |
| `ABT-ADVANCED-HYSTERESIS` | 完了（追補25） |
| `ABT-PVC-RESPONSE` | 完了（追補26・新規追加） |
| `ABT-PMT-RESPONSE` | 完了（追補26・新規追加） |
| `ABT-REST-RATE` | 完了（追補26・新規追加。**搭載機種と設定範囲を追補28で訂正**） |
| `ABT-NEGATIVE-AV-HYSTERESIS-SEARCH` | 完了（追補28・新規追加） |
| `ABT-RATE-RESPONSIVE-AV-DELAY` | 完了（追補28・新規追加） |
| `ABT-RATE-RESPONSIVE-PVARP` | 完了（追補28・新規追加） |
| `ABT-VENTRICULAR-SAFETY-STANDBY` | 完了（追補28・新規追加） |
| `ABT-AUTO-VENTRICULAR-BLANKING` | 完了（追補28・新規追加） |
| `ABT-NOISE-REVERSION` | 完了（追補28・新規追加） |
| `ABT-MAGNET-RESPONSE` | 完了（追補28・新規追加） |
| `ABT-RATE-BRANCH` | 完了（追補29） |
| `ABT-MORPHOLOGY` | 完了（追補29・オリジナルMD側に整理） |
| `ABT-FAR-FIELD-MDTM-MORPHOLOGY` | 完了（追補29・ファーフィールドMD側に整理） |
| `ABT-INTERVAL-STABILITY` | 完了（追補29・AVA／SIHを内包） |
| `ABT-SUDDEN-ONSET` | 完了（追補29・**設定方式を訂正**） |
| `ABT-CHAMBER-ONSET` | 完了（追補29） |
| `ABT-SVT-DISCRIMINATION-TIMEOUT` | 完了（追補29・新規追加。追補30でタイマー起点・後治療を補強） |
| `ABT-SVT-UPPER-LIMIT` | 完了（追補30・新規追加） |
| `ABT-BIGEMINAL-AVOIDANCE` | 完了（追補30・新規追加） |
| `ABT-VT-THERAPY-TIMEOUT` | 完了（追補30・新規追加。**設定範囲とノミナルは未確認**） |
| `ABT-LOW-FREQUENCY-ATTENUATION` | 完了（追補31・新規追加。一覧表27行目の穴を埋めた） |
| `ABT-ST-MONITORING` | 完了（追補31・37。**追補37で全項目を書き直した。`cat` は暫定で「心不全」のまま**） |
| `ABT-SECURESENSETM-RV-LEAD-NOISE` | 完了（追補31。LNDC・設定値を訂正） |
| `ABT-SENSEABILITYTM` | 完了（追補31。設定値がすべて判明） |
| `ABT-DEFT-RESPONSETM` | 完了（追補31・32。波形モード／ショック設定ルールを追補32で追加） |
| `ABT-ATP-PRIOR-DURING-CHARGING` | 完了（追補32。VFゾーンATP・上限レート・充電中の検出） |
| `ABT-DYNAMICTXTM-OVER-CURRENT-DETECTION` | 完了（追補32。60A・OCD・極性の自動変更） |
| `ABT-SHOCKGUARDTM-DETECTION` | 完了（追補32・新規追加。ゾーン設定と Binning） |
| `ABT-EPISODAL-PACING` | 完了（追補32・新規追加） |
| `ABT-POST-SHOCK-PACING` | 完了（追補32・新規追加。一覧表62行目の穴を埋めた） |
| `ABT-CAPACITOR-MAINTENANCE` | 完了（追補32・新規追加） |
| `ABT-SENSEABILITYTM` | 完了（追補31・32・33。**追補33で全項目を書き直した**） |
| `ABT-PVAB` | 完了（追補33・新規追加。旧称 Far Rサプレッション） |
| `ABT-ANTITACHYCARDIA-PACING` | 完了（追補35・新規追加。ATPの全設定値とノミナル） |
| `ABT-HIGH-VOLTAGE-THERAPY` | 完了（追補35・新規追加。同期・Aborted・Non-Committed） |
| `ABT-ALERT-TRIGGER` | 完了（追補36・新規追加。一覧表41行目の穴を埋めた） |
| `ABT-LEAD-MONITORING` | 完了（追補36。HVリード 20〜125Ω・HVLIC） |
| `ABT-CORVUETM` | 完了（追補36。**全項目を書き直した**。トリガ日数・インデックス・測定除外条件） |
| `ABT-STORED-EGM` | 完了（追補38・新規追加。容量・極性・保存時間・エピソードトリガと優先度） |
| `ABT-EXERCISE-ACTIVITY-DIAGNOSTICS` | 完了（追補38・新規追加。`cat` は「モニタ」） |
| `ABT-AUTO-VECTSELECT-QUARTETTM-TEST` | `caution` のみ完了（追補38。CSリードの最終測定極性・世代差） |
| `ABT-LVCAPTM-CONFIRM`／`ABT-BIVCAPTM-CONFIRM` | `caution` のみ完了（追補38。植込み時のPSA測定順序） |
| ペーシング管理 残り6 | RVCap Confirm、Auto Lead Polarity Detection ほか（**LVCap／BiVCap は追補38で `caution` が埋まった**） |
| `ABT-I2I-AV-SYNCHRONY` | 完了（追補41・42・44。**追補44で i2i 設定レベルを 4/7 に訂正し、距離と角度を追記**） |
| `ABT-I2I-LINK-EVALUATION` | 完了（追補44・新規追加。ペアリング／自動評価／設定レベル 1〜7） |
| ICD 残り1 | VF Therapy Assurance（`ecg`・`caution` が空欄） |
| CRT 5 | 未着手（QuickOpt のみ完了。Auto VectSelect は `caution` のみ追補38で記入） |

**分類の内訳**：ICD 24／ペーシング管理 18／CRT 6／センシング 4／モニタ 3／レート応答 2／心房治療 1 ＝ 58件。
（追補41の `ABT-I2I-AV-SYNCHRONY` と追補44の `ABT-I2I-LINK-EVALUATION` を数え直した。以前の「56件・ペーシング管理16」は追補41ぶんの反映漏れ。）
AF Suppression は「心房治療」なのでペーシング管理16件には含まれない。

**RVCap／LVCap／BiVCap Confirm に心室オートキャプチャーの内容を流用しないこと。**
安全域の決め方が違う（他社アプリの記述では「直近の心室閾値＋Pacing Margin の設定値」）。

---

## 5. 残っている宿題（優先順）

1. ~~アルゴリズムのプログラマ操作経路~~ → **保留**（追補27で全件削除。0／204）。
   医師用マニュアルで世代ごとに確定できる見通しが立つまで着手しない
2. **X線識別マーカ**：本体35件中5件のみ（Boston の BSC140／BSC507）。
   **添付文書の【形状・構造及び原理等】に「エックス線写真及び識別番号」の項がある**ので、
   同じ方法で他社も埋められる。**方法は確立済み。**
3. **電池型番・容量・マグネットレート**：AVEIR AR と EMBLEM 以外ほぼ空欄。
   **Accent／Anthem CRT-P 世代は資料から値が取れている**（下記「保留中の値」参照）
4. **Medtronic の MRI 条件**（`mri.json` の verify 10グループ）
   → **Medtronic Azure MRIシリーズ 添付文書（23000BZX00027000）**で解決する見込み
5. **BIOTRONIK 旧世代 9グループ**（`mri.json` の verify）。PMDA未公開。
   バイオトロニックジャパン TEL 03-3473-7485 への照会が正規ルート
6. **CRT-P と ICM の製品登録**。アムヴィア Sky HF-T（承認 30500BZX00069000）から始めるとよい。
   **ICM は追補53で BIOMONITOR III／IIIm の2件を登録した**（BIOTRONIK のみ。Medtronic・Abbott・Boston の ICM 本体は未登録。Abbott は MRI タブにのみ `ABT-ICM` がある）
7. **日本光電扱いのAbbott OEM 8機種**（ニュアンス／ゼネックス／ゼナス／ニュートリノ／
   ニュートリノNxT／ハートマインダー＋／クアドラ＋エクセリス／クアドラ リリーブ）
8. リードのMRI対応（42／868件）。各社の添付文書のリード一覧から拾える
8-2. **`Etrinsa 8 DR-T`（BIOTRONIK）が本体40件に未登録**（追補48で新出）。
    プログラマ画面のタイトルバーに機種名が出るだけで、寸法・電池・寿命などの仕様が資料にないため登録していない。
    添付文書か製品カタログが手に入ったら登録する
8-3. **`Edora 8-T ProMRI シリーズ`（BIOTRONIK）が本体42件に未登録**（追補49で新出）。
    追補49の資料には撮像条件と機能一覧はあるが、**寸法・電池・寿命などの仕様がない**ため登録していない。
    **Edora 8 SR-T ProMRI は Amvia Sky にシングルチャンバがないため現在も販売継続中**なので、優先度は Etrinsa より高い
9. 旧型リードの一次資料（BIOTRONIK／日本光電の PX・DFH・DJP・DNP・PE系が最大の空白）
10. ~~**提案4（レート応答・センサ）の追加可否。**~~ → **追補49-2で `feats` 0行目の `bio` を「Closed Loop Stimulation（CLS）／加速度センサ」に更新して一段落。**
    **さらに追補51-3で `BIO-ACCELEROMETER` を独立エントリとして起こし、完了。**
    加速度センサ側の設定値は `BIO-CLOSED-LOOP-STIMULATION` と用語集7語に収載済み。**残るのは「独立エントリにするか」だけ。**以下は当時の記録。追補25の提案1〜3は追補26で追加済み（163件）。
    Abbott のセンサ本体は未登録のまま。`feats` 0行目「レートレスポンス（生理的センサ）」に5社の値が
    揃っており、BIOTRONIK の CLS が同じ `generic` で登録済みなので、追加するなら一覧表を正として
    `equiv` を作れる。資料の値（センサ閾値・スロープ・リアクション／リカバリー タイム・
    最大センサ レート）は追補25に記録済み

11. **localStorage キーを `cieds_*` から `pacemaker_navi_*` に揃えるかの判断**（追補51で新出）。
    **現状は見送り。**リポジトリ改名では実害がない（localStorage はオリジン単位なのでパス変更では消えない）。
    **揃えるなら旧キーから新キーへ1回だけ移行するコードが要る。**そのまま改名すると利用者の overlay・お気に入り・キャッシュが全部参照できなくなる。
    対象キー：`cieds_master_v3`／`cieds_overlay`／`cieds_master_t`／`cieds_share`／`cieds_fav`／`cieds_recent`／`ciedsnavi.ext.`

### 保留中の値（入れ先が決まれば使える）

Accent／Anthem CRT-P：電池は Greatbatch 社製 **QMR**（リチウム＋SVO／CFx）。
植込み時 3.2V・マグネットレート 100ppm、約4か月後 2.9V・97.9ppm、以後ERIまで100→85ppmに緩やかに低下。
従来のリチウムヨウ素電池は BOL 2.80V／ERI 2.50V／EOL 2.40V。

心室オートキャプチャー搭載の旧機種一覧（世代別の出力仕様つき）も追補23に記録済み。

**Fortify™（追補31に記録）**：CD2235-40Q／CD1235-40Q。寸法 71×40×14mm、質量75g、容積35cc、
エネルギー 45／40J、寿命 7.5〜10.1年／9.1〜10.8年（条件：Rate 60ppm・2.5V・0.5ms・500Ω、
Max Charge 3回/年、100％pace〜0％pace）。Ellipse™ は 39／36J。**本体35件に Fortify は未登録。**
**OptiSense® 1699／1999 心房リード**：電極間隔1.1mm（Tendril ST Optim™ は10mm）、
ペーシング後のファーフィールドR波の平均振幅が94％減少。**リード側の登録有無は未確認。**

---

## 6. 未解決のまま残している食い違い

| 項目 | 内容 |
|---|---|
| **ACap の「2/3」と「2拍」** | 出力を下げる条件について、資料内に「2/3でCAP」と「Accent 2拍／Zephyr 3拍」の2通りの記載。`how` には「3拍中2拍」と書いたが**未確定**。医師用マニュアルで確定するまで動かさない |
| **Cap Confirm の「2倍」** | 他社アプリに「Off または Monitor では出力を測定閾値の2倍（上限5.0V・下限2.0V）」とあるが、メーカー資料に現れない。**未確認** |
| **QuickOpt の 100ms 境界** | 「100ms **以上**なら＋30／100ms **以下**なら＋60」で、ちょうど100msがどちらとも読める。**未確定**。資料どおりの表現で据え置き |
| **SVT アッパーリミットの「Off」** | 追補30の資料は「プログラム-OFF → SVT ディスクリミネータは作動せず」と書くが、画面の選択肢は個別レートの末尾に Off が並ぶ形で「上限を設けない」とも読める。追補32の資料は本項目を **「SVT識別をする上限」** と定義しており、この定義に沿えば Off＝「上限を設けない」と読めるが、Off の動作そのものを明記した記載はやはりない。**未確定・両論併記のまま** |
| **最高感度のノミナル** | Current™ 世代の資料（追補33）は **心房0.2mV／心室Defib 0.3mV**（心室Pacemakerは［Defibと同じ］）と明記。追補32の資料は **心房0.3mV／心室0.5mV**。世代差の可能性が高いが断定できず、**両論を `caution` に併記**。医師用マニュアルで確定する |
| **DFTテスト時の感度設定** | 追補33の資料の手書きメモは「Induction の時は一番鋭くする」、用語集「DFTテスト前のプログラマ設定」は「センシング感度を鈍く(1mV前後)」。目的が違う可能性がある（前者は自己波、後者は誘発VFの確実な検出）。**手書きメモのため収載はしていないが、裏取りが必要** |
| **スタビリティデルタのノミナル**（世代差・軽微） | Current™ 世代の資料のみ **80ms**、Gallant／Entrant（追補29）・Ellipse™（追補31）・Merlin™ PCS 画面（追補32）はいずれも **40ms** で、追補32では本文中に「40ms（ノミナル）」と明記されている。**世代差として整理済み**で、残る不確かさは「Current世代の80msがノミナルなのか例示なのか」だけ |
| **STシフトの判定基準の分母** | 追補37の資料は同じスライドに「**15Vs心拍を評価する**」と「**15心拍のうち、6/8が閾値を超えていたらSTシフトと判断**」の両方を書いており、**分母が15なのか8なのか読み取れない。**追補31で起こした `how` は「8拍中6拍以上（最大15拍）」と解釈しているが、**資料どおりの表現で据え置き。**用語集「STシフト／ノンシフト／非分類」も同じ解釈のまま |
| **STエピソードEGMの保存規則** | 追補37の資料に「**最大5EGMまで保存。First in First out**」という記載と「**5件を保存＝最も古いエピソード1件＋最新のエピソード4件**」という記載の両方がある。**後者なら最も古い1件は上書きされない。両論を `caution` に併記**。**追補38で、本機のストアードEGMの一般ルール（各トリガの「最初の1件」が保護され、残りは FIFO）が判明し、2つの記載はこのルールで矛盾なく説明できることが分かった。**ただしSTエピソードは保護対象の一覧（VT／VF／PMT／マグネット／心房／モーフォロジー／PCショック）に明示されていないため**断定はせず、両論併記のまま** |
| ~~**AVEIR AR2 の比較表の単位**~~ **→ 追補42で解消** | 追補41のセルシートは表の見出しを「消費電力」としていたが、**追補42の資料（AVEIR DR 医局説明会）の原表は `LONGEVITY (YRS)` と明記され、列は Pulse Width（0.25／0.4／0.8ms）、行は % Pacing と Pace Amplitude である。値は「年」で確定した。**日本語資料側の訳による混乱だった。`ABT-I2I-AV-SYNCHRONY` の `params`・`caution`、`ABT-D-AVEIR-AR2` の `longevity_cond`、用語「Aveir AR2」を訂正済み |
| ~~**［i2i＝4/4］の意味**~~ **→ 追補44で解消** | 追補44の資料①の［i2i 設定管理］画面から、**i2i 設定レベルは 1〜7 の範囲**で設定するものと判明した（画面例4、「レベルを7にするとバッテリーの寿命が短くなります」という注意書きあり）。**指示を受けて「i2i＝4/4」を「4/7（設定範囲1〜7）」に訂正済み。**残るのはノミナル値で、これは資料に明記がない |
| **i2i 設定レベルのノミナル** | 画面例は 4 だが、ノミナルであるとは書かれていない。医師用マニュアルで確定する。**未確定** |
| **i2i スループットの許容下限**（追補47で半分解消） | 追補44の資料は 99％（66mm・3°）と 39％（135mm・84°）の実測例を示すだけで、**「何％以上あれば可」という基準を書いていない。**植込み時の目標は「両方向で通信があることの確認」とだけ記載。**追補47の資料は「適切な機能を保証するために、スループットが99%である必要はない」と明記したが、下限値そのものは示していない。あわせて「スループットの％は AV 同期と直接相関しない」とも明記。未確定のまま** |
| **［i2i 強度］の★の意味** | 手動調整画面で★の数（例：RV→RA が2つ、RA→RV が3つ）で表示されるが、**段階数も判定基準も資料にない。追補47の自動評価画面でも ★／★★／★★★ の3段階の例が出るだけ。**ただし追補47には**「信号の過飽和（oversaturation）が発生する可能性があるため、高いほど良いとは限らない」**という注記がある。**未確定** |
| **i2i 自動評価の推奨レベルの決め方**（追補47で新出） | 画面例はテスト4＝★★★／97％、テスト2＝★★／92％、テスト3＝★★／93％ に対し**推奨レベルは両方向とも 5**。**最良スループットのテストとも一致せず、推奨値の算出方法が書かれていない。**「テスト」列の 4/2/3 が設定レベルなのか試行番号なのかも不明。**未確定** |
| **COI 評価とは何か**（追補47で新出） | インテロゲーション中に null マーカが出る原因として「COI評価」が挙げられているが、**COI が何の略で何を評価する操作なのか、資料に説明が一切ない。用語集には収載していない。未確定** |
| **公称バッテリー容量の食い違い**（追補47で新出） | 追補47の資料は **AR 174mAh／VR 241mAh** を「各デバイスの公称容量」とする。既存データは **AR 0.213Ah（Greatbatch 4350）／VR 243mAh**。**VR は 2mAh 差だが AR は 39mAh 差で、丸めでは説明できない。**公称容量と定格容量の違いか、世代差（AR2 の値が混ざった可能性）か判断できない。**`battery_cap` に両論併記した。未確定** |
| **「35%多い」と 37,440回**（追補47・計算のみ確認） | ケーススタディで患者2は患者1より「1分あたりの心臓イベントが35%多い」とされ、「1日あたりの i2i 通信の送信が約37,440回増加」と書かれている。**37,440＝(95−69)bpm×1440分 で一致するが、26/69＝37.7% なので「35%」とは厳密に一致しない。**資料どおりの表記で据え置き |
| **AVEIR DR のエピソード保存と VR の「EGM を保存しない」** | 追補42で **AVEIR VR は EGM を保存しない**と整理した。追補47で **AVEIR DR は AMS エピソード EGM を最大4つ保存できる**ことが判明したが、**保存はノミナル Off** で植込み時に能動的に有効化する必要がある。**矛盾ではなく VR 単独と DR で仕様が違うと解釈**し、`ABT-D-AVEIR-VR` の `diag` は変更していない。**食い違いではないが混同しやすいので残す** |
| **ストアードEGM ［VT エピソード］の［診断］と［検出］** | 追補38の資料は「SVTが長時間続いた場合EGMを使い果たすことが無いか」「VTは残してSVTは残したくないがどうすればよいか」「VTのエピソードトリガにある診断と検出とは‥」と**問いを立てたうえで画面（VT＝診断、VF＝検出）を示すだけで、本文に定義がない。**文脈からは「検出＝検出時点でトリガ／診断＝SVT鑑別でVTと診断されてからトリガ」と読めるが、**明記がないため `params` には選択肢だけを記載した。未確定** |
| **VT治療タイムアウトの設定範囲** | 画面例が資料ごとに違う（追補30＝**60秒**、追補34＝**20分**、追補35＝**Off**）。**追補35で［Off］が選べることが確定し、そのとき［タイムアウト トリガ］は n/a と表示される。**［タイムアウト ゾーン］は VT-2。秒でも分でも設定できるとみられ、Off が既定である可能性は高くなったが、**設定範囲とノミナルは4資料を見ても未確認。**医師用マニュアルで確定する |
| **検出インターバル数** | 追補34の資料は **VTゾーン 6〜100（ノミナル12）／VFゾーン 8・12・16 の3択（ノミナル12）**、追補32の資料は **8〜100 でノミナル VT-1 18／VT-2 16／VF 12**。VFゾーンが3択か連続かという構造レベルの違いで、世代差か誤りか判断できない。**両論を `params` に併記**。追補35の画面例は3ゾーンとも12インターバル。**追補36（Ellipse™ 世代）の2ゾーン画面例は VT 16／VF 12 で、追補32側の値と整合する**が、画面例はノミナルとは限らないため**断定はしていない。範囲そのものは依然として未確定** |
| **キャパシタの最大電圧** | 追補35（Current™ 世代の画面）は **830V**、追補32の資料は **845V**。追補35の資料はゾーン治療の最大出力も 830V／35.0J で自己整合しているため、**世代差として両論を `params` に併記**した。**追補38で Promote™ 世代のHVチャージ集計の電圧区分が「726〜830V」「＞830V」で切られていることが分かり、830V 側の傍証が増えた**が、845V を否定する材料ではないので**両論併記は維持** |
| **キャパシタ メンテナンス間隔の自動変更** | 追補35の資料は「バッテリー電圧が **2.86V** に達すると自動的にインターバルを**1か月**に変更し、さらに **2.6V** に達すると**また3か月**に変更する」と明記。**電池が減るほど間隔が延びる形になり直感に反する。**世代別表は Atlas+／Epic² が「2.86V→1か月」、Epic+ が「2.6V→3か月」で、機種別の値を1文にまとめた可能性もある。**`caution` に両方書いたうえで「医師用マニュアルで裏取りすること」と明記。未確定** |
| **アドバンストヒステリシスのサイクル数** | 「サーチで何サイクル延長するか」と「発作確定までの拍数（Cycle Count）」の2通りに読める。画面上の項目は1つなので同一と解釈したが**未確定** |
| ~~**機能区分（IV型／III型）**~~ **→ 追補49で解消** | VIP搭載＝デュアルチャンバIV型、AICS搭載＝III型は2012年2月時点の記載だった。**追補49の資料「ペースメーカ 機能区分定義・保険償還価格」により現行（2023年7月〜）が確定した。シングルチャンバ・標準型 454,000円（特記事項なし）／デュアルチャンバ（Ⅳ型）593,000円（特記事項なし）／デュアルチャンバ（Ⅴ型）751,000円（定義「エ：頻脈変動感知型抗上室性頻脈ペーシング治療機能を有するものであること」）／シングルチャンバ・リード一体型 1,060,000円（定義「ウ：本体とリードが一体化した構造」）／デュアルチャンバ・リード一体型 1,170,000円（定義「ア：デュアルチャンバ型であること。ウ：本体とリードが一体化した構造」）。CRT-P は QP（Ⅲ型4極用）1,710,000円／BP（Ⅲ型）1,640,000円。****Ⅳ型は「特記事項なし」になっており、2012年当時の「VIP 搭載＝Ⅳ型」という対応づけは現行とは一致しない。**用語集「機能区分（ペースメーカ）」に収載済み |
| **心房オーバードライブの他社名称** | Abbott資料は BIOTRONIK を Overdrive Mode、ソーリンを Atrial Overdriving とするが、一覧表は Overdrive Prevention Pacing／Atrial Overdrive。**一覧表を正として据え置き**。両社の一次資料で確認する |
| **PVC／PMT／レストレートの `device`** | algos は資料どおり `PM`。用語集「レスト レート」は `PM / ICD / CRT` で**不一致**。Abbott の ICD／CRT-D への搭載は資料に記載がなく**未確認** |
| **ペース後AVディレイの最大値テーブル** | 「モード関連Ⅰ」53・54ページに基本レートごとの上限表が2種類あり値が違う（30〜90→350 と 30〜85→350／90→300…）。**世代差と思われるがどちらの世代か明記がない。未確定** |
| **「AVインターバルデルタ」** | レートブランチ画面の Sinus Tachy（V＝A）欄と巻末表（30〜150ms、中央60ms）に項目名だけあり、**本文に説明が一切ない**。AVアソシエーションデルタとは別項目と読めるがレンジもノミナルも同じ。`params` には項目名と範囲だけ入れ、動作は書いていない。**未確定** |
| **オリジナルMD のマッチ数上限** | 本文は「3,4,…20（nominal 7）」だが、ウィンドウ上限も 20 で「分母より分子を大きくできない」。実質上限はウィンドウ設定に依存する。資料どおり 3〜20 と記載 |
| **`feats` 63行目と PVCレスポンス** | 63行目「期外収縮後のポーズ抑制（レート安定化）」の `abt` は「—」のまま。MicroPort の Atrial Pacing on PVC と機能が重なるが行の趣旨が違うため、`generic` を分けた。行を書き足すか新設するかは他社の一次資料待ち |
| ~~`G_FALLBACK` の不一致~~ **→ 追補43で解消（食い違いではなく同期漏れと確定）** | 698語 vs `gloss` 1020語。**追補43の突き合わせで、`gloss` 先頭698語のうち697語が一致し、699語目以降からは1語も採られておらず、並び順も部分列として保たれていることが分かった。したがって「用語集が698語だった時点の全文コピー」であり、抜粋ではない。**陳腐化16件は修正済み。全同期を行うかは §5・§0 の宿題として残る |
| ~~**VA criterion（350ms）の使われ方**~~ **→ 追補49で解消** | 追補49の資料の PMT protection の項に **VA criterion＝350ms（250〜500ms）10ms刻み**と設定範囲が明記され、**検出条件が「VpAs interval が8拍連続して VA criterion より短い」**で向きも確定した。第2条件は「7拍の VpAs interval の偏差が24ms 以内」。**確認**は8拍目に AV ディレイを延長または短縮しても VpAs interval が変化しないこと（設定 AV ディレイ<200ms なら AV 延長＝直近の AV 間隔＋50ms、≧200ms なら AV 短縮＝直近の AV 間隔−50ms）。**停止**は確認後の1拍だけ PVARP を **400ms（VA criterion＋50ms）**に延長すること |
| **Wenckebach response の 130〜167bpm**（追補48で新出。**追補49でほぼ確定**） | 同画面の項目。**設定値なのか、W.I.＝上限レートインターバル − TARP から導かれた計算結果の表示なのか判別できない。**範囲で表示されている点から後者と読める。**追補49の資料では、Atrial upper rate 画面と Auto PVARP 画面のいずれも Upper rate 130bpm に対して Wenckebach response が範囲表示で並び、追補48の Etrinsa 8 DR-T（同じ上限130で130〜167bpm）とは値が違って読める。同じ上限レートで値が変わるのは TARP 依存の計算だからで、「計算結果の表示」という読みを強く支持する。**ただし**スキャン画面が小さく末尾の数値の読み取り精度に限界がある**ため具体値は転記していない。**明記そのものは依然として無いので一行だけ残す** |
| ~~**Atrial refractory period の AUTO**~~ **→ 追補49で実質解消** | 追補49の資料は **「ARP は 225ms に設定されているが、AV ディレイがそれより長い場合には AV ディレイと同じ長さになる」**と明記し、PVC discrimination after As の項でも **「設定された ARP が AV delay より短い場合でも AV delay の間は必ず ARP になる」**としている。**AUTO とは「225ms を基準に、AV ディレイが長ければそれに合わせて自動で伸びる」挙動を指すと読める。**ただし資料が `Atrial refractory period` の AUTO という語そのものを定義しているわけではないため、最終確定は医師用マニュアル待ちとして一行だけ残す |
| **PAV／SAV のタイミング図の 20ms**（追補48で新出） | ペースディレイ／センスディレイをそれぞれ 20ms と見込み、PAV 150ms／SAV 150ms の設定を PAV 170ms／SAV 130ms に変更する例が示されている。**図中の数値の対応づけが判読しづらく、20ms が機種固有の値なのか単なる例示なのかも不明。**用語集には「PAV を長め・SAV を短めにして実効の AV 間隔を揃える調整」という趣旨とこの設定例のみを記載した。**未確定** |
| **EMBLEM A209 のMRI** | 添付文書はA209・A219をまとめて「条件付MRI対応」とするが、本文は「MRI保護モードを有するモデルが含まれる」。**個体ごとに確認**と表示している |
| ACCOLADE の電池寿命 | 公式 8.2／13.2年 と比較表 7.6／12.1年。算出条件が違うため併記 |
| ALIZEA の電池寿命 | 9.3年（添付文書の条件と一致）。公式サイトの13年は別条件（SafeR・低ペーシング率） |
| Cobalt XT DR の寸法 | 2020年資料と2026年比較表で食い違い。新しい方を採用 |
| ULYS・PLATINIUM の取扱会社 | Sorin 時代からの移管時期が未確認のため日本ライフラインのまま |
| **WARAD の窓長の定義** | 追補46の資料は、あるスライドで「先行 P-P（AP/AS 含む）の75％。レート80未満では62.5％」、別のスライドで「Avg 8P-P（後続は Max 500ms）」と書いている。**どちらが定義か決められないので両方の表記を残した。**また「レート80」が心房レートか心室レートかも**明記がない。未確定** |
| **KORA250 と ENO の予測寿命の帰属** | 追補46の資料はスライド見出しが「ENO/KORA250」で、下部ラベルが KORA250（SR 10.7年／DR 11.6年・0.35ms・100％ペーシング／Safe-R）と ENO（DR 8.9年・0.4ms・DDDR 100％／SafeR ON 12.1年）に分かれる。**条件が違うため単純比較できず、どちらの製品の値かも断定できない。**`longevity_cond` に条件と出典スライドを明記して両方を残した。**寸法 41.2×41.5×6.1mm が SR／DR 共通かも不明。未確定** |
| **Xfine TX25D／TX26D の重複登録** | 製品タブに `SOR-L-TX25D`「X Fine TX」と `SOR-L-TX25D-2`「Xfine」が別レコードで存在する（TX26D も同様）。**同一メーカー・同一型番の重複で、方針2が想定するメーカーまたぎの重複ではない。**統合するかは**判断待ち** |
| **リード不具合56％の分母** | 追補45の資料は図3（Kleeman ほか、Circulation 2007）の3群別の内訳（絶縁不良 27/57・23/42・33/47）を示したうえで「絶縁体の絶縁不良が全体の56％」と書いている。**3群を合算した 83/146＝56.8％ と読めるが、資料に計算の明記はない。**用語集には図の数値をそのまま併記した |
| **コネクタ形式一覧の版** | 追補45のコネクタ12形式は「2012/2013 DATA BOOK から」の注記つき。**現行の一覧かは未確認**（DF4・IS4 が載っていない） |
| **アヴェイル用 ECG ケーブルの型番** | 追補42の資料①は「アボットが準備する物品」の表で **3625**、フォローアップ章の「必要なアイテム」で **3626** と書いている。**両表記を `GEAR-PRG-8` に併記して未確定と明示。未確定** |
| **AVEIR VR の予測寿命 10年以上 と 10.3年** | 同じ資料内で概要スライドが「ISO規格の設定において10年以上」、製品スペック表が「10.3 years」。**同じISO規格設定に基づくため、10.3年が具体値・10年以上が丸めた表現と解釈**して `longevity` に 10.3年を入れた。**矛盾ではないと判断済み** |
| **リードレスの抜去成功率** | 追補42の資料①に「植込みから9年間を通して88％以上」と「植込みから最長7年間において80％を超える」の両方があり、資料②は「最長9年間で88％以上」。**いずれも AVEIR ではなく前位機種 Nanostim のデータ**（Reddy VY ほか、APHRS 2022）。**製品タブには収載していない** |
| ~~**MINERVA 試験の参考設定表と Amvia Sky の画面が対応しない**~~ **→ 追補51でほぼ解消**（残るのは「どの機種の試験か」だけ）。**追補51で Amvia Sky の ATP type が Burst／Ramp の2択と確定したため、Burst+ を含む Rx2 は Amvia Sky では設定できず、この表が Amvia Sky の設定そのものではないことが裏づけられた。**以下は追補50時点の記録 | 追補50の資料は「参考）MINERVA試験で使用された設定」として **Rx1／Rx2／Rx3 の3本**の表（Therapy Type＝Ramp／Burst+／Ramp、Initial #1 Pulses＝13、A-S1 Interval＝91%／84%／81%、S1-S2 interval＝Rx2のみ81%、S2-S3 interval＝Rx2のみ20ms、Interval Decrement＝Rx2のみ10ms、# Sequences＝10、Rhythm Change＝On、Time Interval＝Every 7hr）を併記している。**しかし Amvia Sky のプログラマ画面は［1st ATP］［2nd ATP］の2本で、項目名も Attempts／ATP type／Number S1／Add S1／P-S1 interval[%]／S1 decrement／Scan decrement と違い、値も揃わない**（A-S1 Interval 91%/84%/81% 対 P-S1 interval 90%/80%、Time Interval Every 7hr 対 Repetition interval 2h）。**どの機種で行われた試験かも資料に記載がない。Amvia Sky の設定そのものではないと読むのが妥当と判断し、用語集「MINERVA試験（参考設定）」にその旨を明記したうえで表の値は資料どおり収載した。未確定** |
| **ATP type のノミナル**（追補50で新出・**追補51で選択肢は確定**） | **選択肢は Burst／Ramp の2択と確定した**（追補51・「心房ATP治療の種類は Burst/Ramp」）。**MINERVA 参考表の `Burst+` は Amvia Sky の選択肢に無いことも確定した。****残るのはノミナルだけ。**画面例は 1st・2nd とも Ramp。資料が引く EP Europace 2006 は「ATCL≧240ms の AT エピソードでは Burst+ よりも Ramp の有効性が高く、Ramp の高い有効性は最初の6回のシーケンスで明らかになった」としており Ramp がノミナルである傍証にはなるが、**明記がないので断定はしていない。未確定** |
| **AT/AF detection Rate と HVR detection の設定範囲**（追補50で新出・**追補51でほぼ解消**） | **追補51で Attempts（OFF,1〜10）／ATP type（Burst,Ramp）／Number S1（1〜15）／Add S1（OFF,ON）／P-S1 interval（70,75,80,85,88,90,95%）／S1 decrement（5〜40ms）／Scan decrement（OFF,5〜40ms）／Backup stimulation（70,90bpm）／Mode（OFF,VVI）／Repetition interval（OFF,2,4,7,12,24,36h）／Therapy delay（OFF・1〜10min・15〜55min・1〜24h）はすべて判明した。****残るのは AT/AF detection Rate と HVR detection Rate・Detection counter の範囲とノミナルだけ。**AT/AF 側は画面の「HAR limit」に 240／250／261／273／286／300／316／333／353／375／400／429／462／500／545／600 が並び bpm/ms を切り替えられる。**資料に単位・範囲の明記はない。**（当初ここに書いていた ms→bpm の対応づけは方針13①の換算にあたるため、追補53で削除した。アプリ側は追補52-3で削除済み）**未確定** |
| ~~**「治療待機時間」と Therapy delay の関係**~~ **→ 追補51で解消** | **追補51のフローチャートは、01「心房性頻脈の検出」の中に「AT/AF detection［36 out of 48］」と「Therapy delay（治療待機時間）」を並べて置いている。**したがって循環図で「心房頻脈治療終了 → 心房頻脈検出」の矢印に添えられていた「治療待機時間」は **Therapy delay の訳語**であり、Repetition interval や別の内部タイマーではないと確定した |
| **Auto gain（Amvia Sky）の②と③の割り当て**（追補49で新出） | 資料の表は「①最大センサーレートの90%に達する時間が24時間で累積30分を超える → **−1**」「②最大センサーレートの90%に達する時間が7日間で累積60分に到達しない → **±0**」「③『①、②』のどちらにも該当しない → **+1**」と読める。**しかし同じ資料の E シリーズ版は「①24時間で合計90秒を超える → −1」「②24時間で合計90秒に満たない → ±0」「③7日間連続して最大センサーレートに達しない → +1」で、「まったく到達しない → +1（ゲインを上げる）」という向きになっている。**Amvia Sky 版をそのまま読むと「到達不足のときに上げず、中間のときに上げる」という不自然な形になる。**②と③が入れ替わっている可能性があるが断定できないので、資料の表記どおりに収載した。未確定** |
| **Amvia Sky DR-T の償還価格**（追補49で新出） | 既存データは **730,000円**（出典不明）、追補49の資料は **751,000円**（デュアルチャンバⅤ型）。**メーカー資料を優先して 751,000円 に置き換え、`hoken` に旧値も併記した。**既存値の出典が追えていないため一行残す |
| **Amvia Sky HF-T QP／BP の重量・容積**（追補49で新出） | 資料には寸法（53×53×6.5mm）と寿命（10年4か月）しかなく、**重量・容積の記載がない**ため空欄にした。Amvia Sky DR-T 側の 23.2g／11cm³ は既存データのまま残してある。添付文書で確定する |
| **DEXTRUS リードの対象型番**（追補49で新出） | 資料は「**一部の** DEXTRUS リードは BIOTRONIK 社製ペースメーカと組合せることで MRI 対応リードとなる」としか書かず、対象型番の一覧が読み取れない。`mri.json` の `BIO-PM4` には既に **Dextrus Model 4136／4137** が入っているが、**この2つで全部なのかは不明。未確定** |
| **「約3,000人」「既に50名」の分母**（追補49で新出） | DEXTRUS リードの MRI 対応化で撮像可能になる患者数として挙げられているが、**国内なのか全世界なのか、いつ時点なのかが書かれていない。**資料どおりの表記で据え置き |
| **Max. CLS rate の設定範囲とノミナル**（追補52で新出） | 印刷本文は「目標とする最大センサーレート」のみで、画面例が 120bpm。**手書きには「80,90,100,110,120（1刻み）」とあるが手書きは不採用。未確定** |
| **Vp required のノミナル**（追補52で新出） | 資料の記載順（No, Yes）とスライドの強調から No 側とみられるが**明記がない。未確定** |
| **CLS fusion avoidance zone の復帰条件**（追補52で新出） | **休止（リファレンス取得中止）からどう戻るかが書かれていない。**カウンタが −1 側に振れて 16拍中10拍を下回れば戻ると読むのが自然だが、明記はない。**未確定** |
| ~~**CLS の4項目が Amvia Sky にもあるか**~~ **→ 追補52-2で解消** | **Amvia シリーズにも CLS はある**（追補49で DDD-CLS／DDI-CLS を確認済み）。資料が E シリーズを対象機種に挙げているのは、その世代を例に説明しているためだった。**但し書きは削除した。**ただし**4項目それぞれの設定範囲が Amvia Sky と E シリーズで同一かどうかは、どちらの資料にも一覧がない**ため、値の突き合わせは次の資料待ち |
| **CLS response の「80%」の集計期間**（追補52で新出） | 「レート分布を全体の80%になるように自動調整する」の「全体」がどの期間の集計なのか（1日か、統計の全期間か）が書かれていない。**未確定** |
| **BURDEN II の棒グラフと Ikeda らの表の一部**（追補52で新出） | **BURDEN II は表（AT episode を認めた患者の割合）が明瞭に読めるが、下段の棒グラフの数値は判読できない。**Ikeda らも群のサイズ・%AP・AF/AT burden は読めたが、**%VP の各群の値と下段の棒グラフの数値が判読できない。**読めた範囲のみ収載した。**さらに Ikeda らの誌名・巻号・年が資料に印字されていない。未確定** |
| **ブリッジペーシングが設定項目かどうか**（追補51-3で新出） | 追補47の資料に**設定画面・On/Off・設定範囲の記載がない。**自動的に働くものとみられるが、**「設定変更不可」という明記もない。未確定** |
| **ブリッジペーシングの「ほぼ現在のレート間隔」の定義**（追補51-3で新出） | 「ほぼ」が何を指すのか（直前の1拍か、平均か、許容幅があるのか）が書かれていない。**未確定** |
| ~~**Far-field protection の after Vp が after Vs より 50ms 長い理由**~~ **→ 追補52-3で `params` から削除済み（追補53で行を整理）** | **資料に理由の記載はない。**当初書いた「読み」は方針13により追補52-3でアプリから削除した。理由を書くのは資料が来てから |
| **Stability check の判定値が固定かプログラム可能か**（追補51-2で新出） | 8 P-P 中5 P-P・40ms・200ms・80ms のいずれも、**資料に設定画面が現れず「設定変更不可」という明記もない。**固定とみられるが断定できない。医師用マニュアルで確定する。**未確定** |
| **リード位置チェックの判定値が固定かプログラム可能か**（追補51-2で新出） | 32秒毎・13日間・×1.5／×0.75・81ms/80ms・100〜2500Ω のいずれも設定項目として現れない。**Stability check と同じ理由で未確定** |
| **Post ModeSw rate の「+」の基準**（追補51-2で新出） | 「+10bpm」がベーシックレートに対する加算であることは資料の説明（MS 終了後のベーシックレートを上昇させる）から読めるが、**センサ指示レートが基本レートを上回っているときにどちらへ加算されるのかが書かれていない。未確定** |
| **Change of basic rate の刻みの単位**（追補51-2で新出） | 追補49の転記は「+5〜+30ppm、5拍刻み」だが、**追補51で Post ModeSw rate 側が「5bpm刻み」と確認できたことを踏まえると、Change of basic rate 側も本来は「5ppm刻み」の誤記である可能性がある。**追補51の資料には Change of basic rate の刻みの記載がないため訂正はしていない。**未確定** |
| **「確認時間：30s／90s」が設定値か固定値か**（追補51で新出） | Atrial Therapy Sequence 図で 1st Atrial ATP モジュールの後に「確認時間：30s」、2nd Atrial ATP モジュールの後に「確認時間：90s」と書かれているが、**設定項目としては［1st and 2nd ATP］画面に現れない。****治療成功の定義が「30秒以内」であることと 1st ATP 後の30秒は一致するが、2nd ATP 後がなぜ90秒なのかの説明がない。未確定** |
| **Scan decrement の作動の向き**（追補51で新出） | 設定範囲（OFF, 5〜40ms）は判明したが、**資料に Scan の動作そのものの説明が一切ない。**当初用語集に書いた他社からの類推は**追補52-3で削除し、用語集は「作動の説明は資料になく設定範囲のみ」としてある。**医師用マニュアルで確定する。**未確定** |
| **Repetition interval の起点の定義**（追補51で新出） | 図は起点を「最後の Atrial ATP therapy もしくは最後に抑制された治療」と書くが、**「抑制された治療」に Blocked も含むのか Inhibit のみなのかが読み取れない。**Blocked は再プログラムまで再開しないので Inhibit のみと読むのが自然だが、明記はない。**未確定** |
| **48時間ルールの起点**（追補51で新出） | Blocked の条件「AT/AF エピソードが48時間以上持続したとき」の48時間が、**エピソード検出時からなのか最初の治療からなのかが書かれていない。未確定** |
| **「600 attempts/月」の位置づけ**（追補51で新出） | バッテリー試算（−1.1%）の前提として置かれているが、**これが標準的な作動量の想定なのか、上限（1,000アテンプト/30日）に対する例示なのかが書かれていない。**1,000/30日より少ないので「多めの想定」と読んだが、資料に根拠の記載はない。**未確定** |
| ~~**ERAF の引用文献の誌名**~~ **→ 追補53で解消（指示により誌名を削除）** | スライド下部は「Volume 113, Issue 16, 25 April 2006; Pages 1933-1941」とだけあり、**誌名が資料本文に出ていない。**追補51で私が同定して書いた「Circulation」を、**指示を受けて用語 `ERAF`・`BIO-POST-MODESW-RATE` から削除し、資料どおりの巻号・ページと「誌名は資料に記載がない」に置き換えた** |
| **Ramp vs Burst+ の試験の対象機種**（追補51で新出） | EP Europace 2006;8(7):465-473 の紹介スライドは「DDDRP device」とあるだけで機種が書かれていない。**Burst+ が Amvia Sky に無い以上 BIOTRONIK 機ではない可能性が高いが、本文からは断定できない。**用語集では機種を書かず所見のみを記載した。**未確定** |
| **BIOMONITOR：Confirmation time の値**（追補53で新出） | AF sensitivity の表は Low／Medium／High とも **6**。Cryptogenic stroke テンプレートの注記は「Confirmation time は『**1 ⇒ 2**』に変更することを推奨」で、テンプレートの値が 1 であることになる。**テンプレートだけ別の値なのか、どちらかの誤記なのか資料にない。未確定** |
| **BIOMONITOR：HVR counter の図の値**（追補53で新出） | 検出ルールの図は HVR counter＝**4**。プログラマ画面の選択肢は **8・12・16・20・24・32・40** で 4 がない。**未確定** |
| **BIOMONITOR：患者トリガの記録時間**（追補53で新出） | Remote Assistant III の章は**最大7分30秒（前7分＋後30秒）**、診療報酬早見表は「患者トリガ（**310秒/件**）」。**未確定** |
| **BIOMONITOR：Palpitations テンプレートの Transmission**（追補53で新出） | ProgramConsult の表は AF の Transmission **ON**／Patient trigger **ON**。Home Monitoring の章の画面例（Palpitations に設定した場合）は AF **Detect. only**／Patient trigger **OFF**。**未確定** |
| **BIOMONITOR：ノミナルと設定範囲**（追補53で新出） | 「6つの検出エピソード」の表（AF Medium／HVR 180／Brady 40／SRD 50／Asystole 3／Patient trigger ON）がノミナルかどうか明記がない。Sensing filter（画面例 10Hz）・Signal filter（画面例 0.5Hz）も同じ。**HVR limit／Brady zone limit／SRD rate decrease／DHP の設定範囲は資料にない。未確定** |
| **BIOMONITOR IIIm の MRI 撮像条件**（追補53で新出） | スペック表は IIIm・III とも「MRI-Conditional 1.5T/3T FBS」。**撮像条件と手順の章は BIOMONITOR III 表記**で、IIIm が同一と明記した記載はない。**`BIO-ICM1` は verify 付き。添付文書で確定する。FBS の展開も資料にない** |
| **BIOMONITOR の保険区分と予測寿命の「*」**（追補53で新出） | 資料は機能区分（標準型 394,000円／特殊型 451,000円）の定義を示すが、**本品がどちらかを文章で明記していない**（製品の `price` は空欄）。予測寿命 IIIm 5.5年*・III 4年* の **\* の注記本文が資料にない**。**未確定** |
| **ICM 診療報酬早見表の階段の境界**（追補53で新出） | どのセルが 90点／180点かの境目はスキャンで判読しにくく**転記していない**（計算による補完もしていない。方針13）。3つの規則文（0点／90点＝30分未満／180点＝30分以上）と注記だけを収載 |
| **ICM 市場推移の図の「Confirm rx launch in Jun by ABT」**（追補53で新出） | 矢印が2018年と2019年の間にあり、**年を読み取れない**。用語 `植込型心電計` にそのまま記載 |
| **BIOMONITOR の Resting rate period**（追補53で新出） | 画面に Start resting period 02:00／Resting period duration 4h が並ぶが、**何に使う項目か資料に説明がない。未確定** |
| **LSP202V の世代** | `mri.json` は LSP112V と LSP202V の両方を「Aveir VR」として登録している。**LSP202V が第2世代の心室用（VR2）かは追補42・44の資料でも判明せず。**ただし追補44の資料①のプログラマ画面（［LP 植込み／交換］）には「既存心室：LSP202V／VVI／50 min-1／2.5V／0.4ms、電池電圧3.0V」と表示されており、**DR システムの心室側デバイスとして使われることは確認できた。**世代は**未確定** |

---

## 7. 直近でやったこと

### 2026-09-10（追補53）

**BIOTRONIK「BIOMONITOR IIIm ～製品トレーニング資料～」（76スライド）を反映。4タブすべてを更新。手書きは不採用。**

- **アルゴリズム 204 → 214件**（`algos.json` v39）。**ICM のアルゴリズムを初めて起こした**（BIOTRONIK 10件、分類「ICM」）。資料の見出し（AF detection／HVR／Bradycardia／SRD／Asystole／Patient trigger／Sensing／Intelligent Memory Management／ProgramConsult／Home Monitoring）ごとに1件。
  **AF detection enhancement と AF with ectopy detection は `BIO-ICM-AF-DETECTION` に、ノイズの検出は `BIO-ICM-SENSING` に収めた**（追補52-2 の CLS と同じ考え方）
- **用語集 1219 → 1247語**（`data.json` v53）。新規28語は**末尾に追加**（`G_FALLBACK` の先頭698語の並びを崩さない）。**見出しは資料にある語だけで作った**。`BIOMONITOR IIIm` を全面書き直し、6語に加筆
- **製品 939 → 943件**（`products.json` v16）。本体2件（分類 ICM）、関連機器2件（新分類「ICM付属品」）、既存の BIOTRONIK 関連機器3件に加筆。**償還価格は区分が明記されていないため空欄**
- **MRI 56 → 57グループ**（`mri.json` v13）。`BIO-ICM1`（verify 付き）
- **`index.html`**：製品用 `CAT_ORDER` に「ICM付属品」を追加、`G_FALLBACK` の5語を同期（698語のまま、食い違い0件、script の構文チェック済み）。`feats` は変更なし
- **指示により ERAF の誌名「Circulation」を削除**（`ERAF`・`BIO-POST-MODESW-RATE`）。**§6 の古い3行を整理**
- **追補52-4 の数値例7箇所は本資料に該当なし**
- **気づいたが未着手**：`feats` 14行目の償還価格が古い（BIO 730,000円のまま）／`BIO-STABILITY-CHECK` に推測に見える文が4つ（§0 の次にやること17・18）

### 2026-09-06（追補52-4）

**指示を受けてアルゴリズム全204件を対象に推測で書いた記述を洗い出し、9箇所を訂正した**（用語集2語も）。

- **走査は2段階**。①推測を示す語23種（`と読める`／`とみられる`／`と考えられる`／`類推` ほか）で全件・全フィールド → **4箇所該当**。
  ②方針13の4類型に対応する語（`に相当する`／`換算`／`たとえば`／`（例：`／`他社の`／`まず`／`疑う`／`手がかりになる` ほか）→ **のべ46フィールド該当**。**すべて1件ずつ内容を確認した**
- **訂正した9箇所**：`ABT-DYNAMICTXTM-OVER-CURRENT-DETECTION`（治療成功と読める）／`ABT-SVT-UPPER-LIMIT`（Off の意味の推論）／
  `ABT-VT-THERAPY-TIMEOUT`（秒/分の単位の推定）／`ABT-PVAB`（Off にできない設計意図の推定）／
  **`ABT-ANTITACHYCARDIA-PACING`・`ABT-SHOCKGUARDTM-DETECTION`（「375×0.85＝318ms／Min BCL 320ms」という私が作った数値例。検出インターバルと BCL％ は資料にあるが Min BCL の値は私が置いたもの）**／
  `ABT-RATE-BRANCH`（Slow VT の臨床例と If Any／If All の作例）／`ABT-BIGEMINAL-AVOIDANCE`（200ms と 500ms）／
  `ABT-I2I-LINK-EVALUATION`（100件送ったとき）／`BIO-RATE-FADING`（場面設定）
- **用語集2件**：`最小ペーシングインターバル（160ms）` の「約375bpm に相当する」／`HAR limit` の「（＝200bpm）」。**どちらも私の単位換算**
- **判断に迷って残した8箇所を notes に書き出した。**資料由来と判断した数値例7つ（79・108・378・271・107・4.2ms など割り切れない値や画面の英文）と、
  一般的な臨床の目安1つ（`最大センサ レート` の（220−年齢）×0.9）。**元資料が手元になく確定できていないので、次に資料が来たときに突き合わせる**
- **方針13の④を実運用に合わせて書き直した。****「資料にある動作・所見の読み方の案内」は残し、「資料にない事実・数値・手順の創作」を禁じる形にした**
- **`index.html` は変更なし**（訂正した2語は `G_FALLBACK` の698語に含まれない）

### 2026-09-06（追補52-3）

**指示を受けて用語『CLS 使用時の心室ペーシング抑制機能』を削除（1220 → 1219語）。あわせて推測で書いていた記述を21箇所洗い出して削除・書き換えた。**

- **削除した用語は、追補52-2で私が「誤読しやすい点を独立させる」という理由で追加したもので、資料に対応する項目名がない私が作った見出しだった**
- **推測の洗い出しは `と読める`／`とみられる`／`と読むのが自然`／`類推`／`読みにとどめ`／`と考えられる` を機械的に走査して行った**（追補51以降に私が足した箇所が対象）
- 削った主なもの：**HAR limit の ms→bpm 換算**（`BIO-ATRIAL-ATP`・用語2語）／**CLS response の計算例 72.6ppm**／**Vp required のノミナルの推定**／
  **CLS の「レートが上がらないときに疑う順序」**（私が組み立てた4段）／**リード抵抗値の 1,200Ω の例**（そもそも例として成り立っていなかった）／
  **Scan decrement の他社からの類推**／**ブリッジペーシングの運用助言**／**加速度センサの「乗り物の振動」**
- **`BIO-HVR-DETECTION` の「心房 190bpm の AT が 1:1 伝導すれば…」は、追補51で自分が「手書きなので不採用」と書いた書き込みを言い換えて復活させてしまっていたもので、削除した**
- **`index.html` を再同期**（用語『CLS』の書き換えに伴う。698語のまま。削除した語は `G_FALLBACK` に入っていなかった）
- **方針13「推測を書かない」を §2 に明文化した**

### 2026-09-06（追補52-2）

**指示を受けて追補52の反映を3点訂正。アルゴリズム 209 → 204件**（`algos.json` v34 → v35）。

- **① 新規5件を `BIO-CLOSED-LOOP-STIMULATION` に統合した。**`how` を「成り立ち」「測定のしかた」「リファレンス波形の作成と取得ルール」「設定パラメータ（4項目）」「融合収縮を避けるためのアルゴリズム」の5つの見出しに区切って全内容を収めた。
  **内容は落としていない**（消えたのは5件固有の `scope`／`generic`／`equiv` のみ）。**削除した5 ID への参照が0件であることを機械確認済み**（4エントリの参照を `BIO-CLOSED-LOOP-STIMULATION` に張り替え）
- **② 「CLS の代替は IRS plus もしくは Positive」という私の書き方が誤りだったので訂正した。**
  **資料が言っているのは「Vp suppression（心室ペーシング抑制機能）の代わりに何を使うか」であって、CLS の代替ではない。**
  CLS を使うと Vp suppression が使えなくなる → **それでも心室ペーシングを減らしたいなら Vp suppression の代わりに IRS plus か Positive を置く**、という構図。
  4エントリ・用語集2語・製品3件・HANDOFF を訂正し、**用語集に『CLS 使用時の心室ペーシング抑制機能』を追加**（1219 → 1220語）
- **③ Amvia シリーズにも CLS があることを明記した。**追補52で付けた「4項目が Amvia Sky にもあるかは未確定」という但し書きは過剰だった。
  **追補49の時点で Amvia Sky に DDD-CLS／DDI-CLS があることを確認済みだった。**`models`・製品3件の `diag`・用語集2語を直し、**§6 の未確定から1件を削除**
- **`index.html` を再同期**（用語集『CLS』を再度書き換えたため。件数は698のまま。`G_FALLBACK` と `data.json` の食い違いが0件であることを機械確認済み）
- **反省点**：②③はどちらも**「資料に書いていないことを補おうとして逆に間違えた」**類のミス。
  とくに③は、**新しい資料の射程を狭く取るあまり、既に確定している情報（追補49）と突き合わせなかった。**
  **資料の対象機種欄から「載っていない機種には無い」方向の推定をするときは、既存データに反証がないかを先に確認する**

### 2026-09-06（追補52）

BIOTRONIK「Biotronik Product Brady  Closed Loop Stimulation」（日本語・スキャンPDF・22スライド）を反映。
**アルゴリズム 204 → 209件、用語集 1199 → 1219語、製品は 939件のまま3件を加筆。MRI は変更なし。`index.html` を更新（方針12）。**

- **`BIO-CLOSED-LOOP-STIMULATION` を全面書き直し**：循環制御の閉鎖ループ／不完全閉鎖ループ／**RV tip to Can（unipolar）・電極周辺 約1cm²**／
  **単極インピーダンスと dp/dt の相関**（Intrinsic R²＝0.9252／VVI paced R²＝0.9247。PACE 2000;23(Pt II):1502-1508）／**面積の差でレートを決める**／
  運動様式・年齢別トレンド／Color-word test／**透析困難症例**（CLS OFF は血圧183→154でレート70→69、CLS ON は血圧183→179でレート74→84。横浜労災病院提供）／
  **BURDEN II Study**（451例。>1〜<5分で Overdrive 38.1%／DDDR 33.9%／CLS 27.0%、P 0.02 など）／**Ikeda ら**（AF/AT burden は FP 1.6／CLS 0.0／Non-CLS RM 1.0）
- **新規5件**：`BIO-CLS-REFERENCE`（50〜300ms・16発／安静時のみ／Vp 用は AV ディレイ2拍短縮／**Vp required が Yes だと Vs リファレンスは更新されない**）／
  `BIO-CLS-RESPONSE`（**BR＋[（MCLR−BR）×係数]、0.21／0.26／0.33／0.41／0.51、ノミナル Medium。上限は変えない**）／
  `BIO-CLS-RESTING-RATE-CONTROL`（**+20ppm、Off・+10〜+50ppm、10ppm刻み。神経調節性失神では OFF か 100ppm 程度まで**）／
  `BIO-VP-REQUIRED`（**59サイクル → 60・61拍目に延長 → 4セット8拍中6拍で移行／8拍中7拍連続 Vp でキャンセル／180拍連続 Vp で再評価**）／
  `BIO-CLS-FUSION-AVOIDANCE`（**常に On。40ms の zone に Vs があれば +1／なければ −1、最新16拍中10拍以上で CLS 休止**）
- **既存6件を訂正／加筆**：**`BIO-VP-SUPPRESSION`／`BIO-INTRINSIC-RHYTHM-SUPPORT`／`BIO-POSITIVE-AV-HYSTERESIS-WITH-SCAN-REPETIT` の「CLS の代替は IRSplus」を
  「IRS plus もしくは Positive」に訂正**（追補49の記載を追補52の資料で更新）／`BIO-DYNAMIC-AV-DELAY`（自己 PQ 間隔より設定 AV ディレイが長い場合は非推奨）／
  `BIO-ACCELEROMETER`（CLS の安静判定に使われている関係＋センサ比較表）
- **用語集は新規20語・既存1語（`CLS`）を訂正。****訂正した `CLS` が `G_FALLBACK` の698語に含まれていたため `index.html` を同期した**（件数は698のまま）
- **製品は Amvia Sky の3件（DR-T／HF-T QP／HF-T）の `diag` に CLS の要約を追記。**
  **本資料が「Product Brady」＝徐脈デバイスの資料であるため、CLS を持つ ICD／CRT-D 側への転記は資料の射程を超えると判断して見送った**
- **資料の射程に注意**：**「設定パラメータ」のスライドの対象機種は E シリーズで、Amvia Sky の記載がない。**4項目が Amvia Sky にもあるかは未確定として `models` と `diag` に但し書きを入れた

### 2026-09-06（追補51-3）

**指示を受けて、これまでストックしていた新規エントリの提案をすべて起こした。アルゴリズム 201 → 204件**（`algos.json` v32 → v33）。

- **notes 全106エントリを機械的に走査して未処理の提案を洗い出したところ3件だった**（HANDOFF §5 の宿題一覧とも突き合わせ。温度センサは見送り確定のため対象外）
- **`ABT-BRIDGE-PACING`**（ペーシング管理／PM（リードレス））：**V2A 通信が1回中断したときに心房ペースを1回だけ提供。ほぼ現在のレート間隔。**
  **中断はほとんど6秒未満**なので1拍つなげば復帰する。**1回＝ブリッジペーシング／継続＝「@」（comm マーカ）**。`ecg` に null マーカ「o」の読み方を収載
- **`BIO-BLANKING` を2件に分割**（センシング）：**`BIO-FAR-FIELD-PROTECTION`**（after Vs 100ms／after Vp 150ms、100〜225ms 25ms刻み。E シリーズは10ms刻みで最大220ms。**Ars(FFP) マーカは E-series のみ**。FFRW の Type I／Type II）と
  **`BIO-VEN-BLANKING-AP`**（30ms、30〜70ms 5ms刻み。**Safety window との一次防御／二次防御の関係**。Atrial ATP の 6.0V/1.5ms 固定によるクロストーク事前確認）。**元の `BIO-BLANKING` は削除**（参照は `id` 以外に無いことを確認済み）
- **`BIO-ACCELEROMETER`**（レート応答）：**Max. sensor rate 120ppm（80〜180ppm、10ppm刻み）／Sensor gain・Sensor threshold ともノミナル Medium／Rate increase 2ppm/cycle・Rate decrease 0.5ppm/cycle。**
  **信号処理の3段構え（増幅→閾値→変化量）**を整理。**Auto gain の論理が Amvia Sky と E シリーズで揃わない点は未確定として両方併記**。Sensor optimization（最長16分）も収載
- **切り出しでは元エントリに要約を残し、相互参照を張った**（`ABT-I2I-AV-SYNCHRONY`／`BIO-SAFETY-WINDOW`／`BIO-CLOSED-LOOP-STIMULATION`／`BIO-STABILITY-CHECK`）
- **用語集・製品・MRI は変更なし**（1199語／939件／56グループ）。**`feats` 未変更のため `F_FALLBACK`・`G_FALLBACK` とも同期不要**
- **`maker` 別では BIOTRONIK 40 → 42件、Abbott 58 → 59件。`cat` 別ではセンシング 11 → 12件、レート応答 11 → 12件、ペーシング管理 62 → 63件**
- **これで notes に記録されていた新規エントリの提案はすべて処理済み。ストックは空である**

### 2026-09-06（追補51-2）

**指示を受けて方針11（アルゴリズムの件数を勝手に増やさない）を撤回。追補51の資料から4件を新規エントリ化し、アルゴリズム 197 → 201件**（`algos.json` v31 → v32）。

- **方針11は過去のトークで私が自分に課した運用ルールであって、利用者からの指示ではなかった。**§2 の方針11を書き換え、§0 の「踏んではいけない地雷」からも件数の記述を外した
- **新規4件（BIOTRONIK は 36 → 40件。`cat`「心房治療」は 14 → 18件）**
  **`BIO-STABILITY-CHECK`**（8 P-P 中5 P-P で 40ms 以内なら安定／8 P-P 中4 P-P で 40ms 超なら不安定／3 P-P が 200ms 未満なら治療保留／アンダーセンシングは2倍読み替えで 80ms 以内なら Stable）
  **`BIO-LEAD-POSITION-CHECK`**（Daily＝32秒毎測定・1日平均・13日平均の×1.5／×0.75・ApVs 81ms/80ms／直前＝100〜2500Ω。**Daily の異常は Blocked、直前の異常は Inhibit という非対称**）
  **`BIO-HVR-DETECTION`**（**治療直前は Inhibit・治療後30秒以内は Blocked**。Remark は “No therapy delivered due to ven. acceleration”。Atrial ATP 設定時は自動で ON）
  **`BIO-POST-MODESW-RATE`**（+10bpm・+5〜+50bpm 5bpm刻み／+1min・1〜30min 1分刻み。**目的は ERAF の予防**。Amvia Sky は Atrial overdrive 非搭載のためこれが代役。**Change of basic rate は MS 中・Post ModeSw rate は MS 終了後**）
- **`BIO-ATRIAL-ATP` と `BIO-MODE-SWITCHING` の `how` 末尾に相互参照を追記**（要約は元エントリに残した）
- **用語集・製品・MRI は変更なし**（1199語／939件／56グループ）。**`feats` 未変更のため `F_FALLBACK` の同期不要。`G_FALLBACK` も不要**
- **`pathway` は4件とも空**（方針9のまま。0／201）
- **今後の追加候補3件を notes に記録**：`ABT-BRIDGE-PACING`／`BIO-BLANKING` の心房・心室分割／`BIO-ACCELEROMETER` の切り出し

### 2026-09-06（追補51）

BIOTRONIK「Amvia Sky シリーズ DR-T/HF-T QP/HF-T BP “More Options for Device Management” Atrial ATP」（日本語・スキャンPDF・24スライド）を反映。
**追補50の2スライドはこの資料の抜粋だった。****アルゴリズム・用語集・製品の3タブを更新。MRI は変更なし。件数は増やしていない。**

- **`BIO-ATRIAL-ATP` を全面的に書き直した**（`how`／`params`／`models`／`ecg`／`pitfall`／`caution` を入れ替え）。
  **4段階フローチャート**（AT/AF detection［36 out of 48］→ 治療前チェック → Burst/Ramp 治療 → AT/AF termination［20 out of 24］）、
  **Daily checks と Last minute checks の二重構造**、**Stability check（8 P-P 中5 P-P で 40ms 以内）**、**アンダーセンシング補正（2倍読み替えで 80ms 以内）**、
  **Position check（100〜2500Ω）**、**ApVs 81ms/80ms 分岐**、**Repetition through rhythm change の判定（8インターバルの中央値・10分毎に再計算・50ms 以上の解離）**を収載
- **設定範囲が全面的に判明**：Attempts OFF・1〜10／**ATP type＝Burst／Ramp の2択**／Number S1 1〜15／Add S1 OFF・ON／P-S1 interval 70〜95%／S1 decrement 5〜40ms／
  Scan decrement OFF・5〜40ms／Backup stimulation 70・90bpm／Mode OFF・VVI／Repetition interval OFF・2・4・7・12・24・36h／**Therapy delay OFF・1〜10min・15〜55min・1〜24h**／**出力 6.0V/1.5ms 固定**
- **作動制限の一覧表を `caution` に収載**（Blocked 3条件＝再プログラム／Inhibit 6条件＝自動再開。**最小ペーシングインターバル 160ms**）
- **電池への影響 −1.1%（10年 ⇒ 9年10か月）**、**治療成功は「30秒以内の終了」のみ**、**植込み後1か月経過してから有効化を推奨**を収載
- **他6件に加筆**：`BIO-ATRIAL-CAPTURE-CONTROL`（ACC が Atrial ATP の前提条件）／`BIO-MODE-SWITCHING`（**Post ModeSw rate の刻みを「5拍刻み」→「5bpm刻み」に訂正**・ERAF 予防・80ppm 検討）／
  `BIO-BLANKING`（6.0V/1.5ms 固定によるクロストーク事前確認）／`BIO-CLOSED-LOOP-STIMULATION`（アップストリーム治療・心房ペーシング率1%増で AF リスク2%減）／
  `BIO-INTRINSIC-RHYTHM-SUPPORT`（心室ペーシング率1%増で AF リスク3%増・拡張期MRの反例）／`BIO-DYNAMIC-AV-DELAY`（AV 230ms 以上で拡張期MR）／`BIO-OVERDRIVE-PREVENTION-PACING`（ERAF 対策は Post ModeSw rate が代役）
- **用語集 1172 → 1199語**（新規27語・既存11語を訂正／加筆）
- **製品は 939件のまま。**`BIO-D-AMVIA-SKY-DR` の `diag` を差し替え、**`BIO-D-AMVIA-SKY-HFT-QP`／`BIO-D-AMVIA-SKY-HFT-BP` にも Atrial ATP を追記**（資料の表題が対象機種を DR-T/HF-T QP/HF-T BP と明示）
- **§6 の未確定4件が解消**（ATP type の選択肢／設定範囲／MINERVA 参考表／治療待機時間＝Therapy delay）
- **`G_FALLBACK` の同期は不要**（訂正11語はいずれも698語に含まれない。機械的に照合して確認）。**`feats` 未変更のため `F_FALLBACK` も不要**
- **リポジトリを `ori287/cieds` → `ori287/pacemaker_navi` に改名。`index.html` の改修は不要と確認**（絶対URLのハードコードなし・相対パス・SW/base/manifest なし）

### 2026-09-06（追補50）

BIOTRONIK「Amvia Sky ～ Atrial Anti-tachycardia pacing ～ Atrial ATP の設定」（日本語・2スライド）を反映。
**アルゴリズム・用語集・製品の3タブを更新。MRI は変更なし。件数は増やしていない。**

- **`BIO-ATRIAL-ATP` の `device` を「ICD / CRT-D」→「PM / ICD / CRT」に訂正**し、`models` の先頭に Amvia Sky DR-T を置いた
- **`how` に治療の循環**（心房頻脈検出 → リード位置チェック → 心調律安定性チェック → ATP治療 → 繰返し → 治療終了 → 治療待機時間）、
  **`params` に画面例の全値**（AT/AF detection 170bpm、HVR detection 200bpm・counter 8、1st/2nd ATP 10*Ramp、Number S1 13、P-S1 interval 90%/80%、Backup stimulation 70bpm・VVI）を入れた
- **`pitfall` に「HVR detection は心室レートでのみ評価するため、1:1AT などでは治療が行われない」**を明記
- **用語集 1162 → 1172語**（Atrial ATP Therapy／AT/AF detection／HVR detection／Therapy delay／Repetition interval／1st ATP・2nd ATP／
  Backup stimulation（Atrial ATP）／P-S1 interval／Number S1・Add S1／MINERVA試験（参考設定））
- **製品は 939件のまま。**`BIO-D-AMVIA-SKY-DR` の `diag` に Atrial ATP Therapy の検出・治療設定の要約を加筆
- **既存用語の訂正がなかったので `G_FALLBACK` の同期は不要。`feats` も触っていないので `F_FALLBACK` の同期も不要**

### 2026-09-06（追補49-2）

追補49の反映後、**指示を受けて2件を実施した。**

- **アルゴリズム 190 → 197件。**BIOTRONIK 7件を追加（`BIO-NIGHT-RATE`／`BIO-DYNAMIC-AV-DELAY`／`BIO-SAFETY-WINDOW`／
  `BIO-EMI-MODE`／`BIO-MAGNET-RESPONSE`／`BIO-PMT-PROTECTION`／`BIO-BLANKING`）。
  **いずれも①の形（一覧表を正）で、`generic` は対応する Abbott エントリと同一文字列にしてある。`cat` は既存値のみ・`pathway` は全件空欄。**
  **`BIO-BLANKING` だけは資料の章立てに合わせ、心房側（Far-field protection）と心室側（Ven. blanking after Ap）を1エントリにまとめた。分割するかは判断待ち**
- **`feats` 0行目「レートレスポンス（生理的センサ）」の `bio` を「Closed Loop Stimulation（CLS）／加速度センサ」に変更**し、
  `note` に加速度センサ側の設定値を書き足した。**方針8に従い `index.html` の `F_FALLBACK` を同期**（差分は0行目のみで、追補32以来の65行一致を再確認）
- **`index.html` 476,280 → 476,960 バイト。`G_FALLBACK` は追補49の11語同期のまま変更なし**

### 2026-09-06（追補49）

BIOTRONIK「Amvia Sky & Edora  DR-T/SR-T/HF-T QP/HF-T BP  BIOTRONIK Product Brady Parameter」（日本語・スキャンPDF）を反映。**4タブすべてに手を入れた。**
**手書きは一切採用していない。**

- **アルゴリズム 190件のまま。**16件を大幅に加筆・訂正し、他社11件の `equiv` を BIOTRONIK 実名で充填した
- **`BIO-RATE-FADING` を全面的に訂正。**既存の `how` は Medtronic の RDR の説明を当てはめたものだった。
  **正しくは Target rate＝（直近4P-P間隔の平均）−10ppm を作り、達すると Back up rate として心房ペーシングを行う方式。介入レート・介入時間という項目は存在しない。4連続以上の As マーカが必要**
- **`BIO-RATE-STABILIZATION` の `name` を「Rate Stabilization during Mode Switching」に訂正**（Target rate＝直近4R-R平均−10ppm、ノミナル OFF）
- **`BIO-AUTOMATIC-SENSITIVITY-CONTROL` の `device` を「ICD / CRT-D」→「PM / ICD / CRT」に、
  `BIO-PVC-DISCRIMINATION-AFTER-AS` の `device` を「CRT」→「PM / ICD / CRT」に変更**（いずれもブラディ全世代に搭載されていることが判明したため）
- **追補48で「画面例でありノミナルとは限らない」と留保していた4値がノミナルだったと確定**
  （Far-field protection after Vs 100ms／after Vp 150ms、Ven. blanking after Ap 30ms、PVARP after PVC 375ms）
- **§6 の未確定4件が解消**（VA criterion／Atrial refractory period の AUTO／Wenckebach response／機能区分）
- **用語集 1116 → 1162語**（新規46語・訂正16語）
- **製品 937 → 939件。**`BIO-D-AMVIA-SKY-DR` の寸法・寿命・価格を訂正（10.5年 → **11年3か月**、730,000円 → **751,000円**）し、
  **Amvia Sky HF-T QP／Amvia Sky HF-T を新規登録（本体 40 → 42件）。製品タブ初の CRT-P 登録**
- **MRI 56グループのまま。**`BIO-PM4`・`BIO-CRTP3` に**全身撮像条件（Full-body）と部分撮像条件（Partial）の2段構え**と MRI AutoDetect を、
  `BIO-PM2`・`BIO-PM3` に **E シリーズの MRI AutoDetect は条件が異なる**という注記を加筆。**判定（tesla・適合リード・verify）は一切変更していない**
- **`G_FALLBACK` を11語同期。**`F_FALLBACK` と `CAT_ORDER` は変更なし

### 2026-09-05（追補46）

MicroPort CRM「総合製品説明資料 Part1」CJ110021 ver1.0（2022年1月・36スライド）を反映。**4タブすべてに手を入れた。**

- **アルゴリズム 188 → 190件。**新規は `MCP-WARAD`（`cat` センシング）と `MCP-AIDA`（`cat` モニタ）。
  **WARAD は「用語集に語だけあってアルゴリズムの実体がない」穴だった**（追補45と同じ探し方）
- **`MCP-MODE-SWITCHING` の名称を「Fallback Mode Switch（FMS）」に訂正。**
  **第1基準＝32心周期中28周期（87.5％）／第2基準＝32心周期中18周期（56％）が2ブロック連続／
  移行後は12サイクルごとに30msエスケープ延長／復帰は24サイクルAp-Vp＋直近12サイクルPACなし**
- **`MCP-SAFERTM`**：AAI→DDD の4基準（Ⅲ度＝2連続R波欠如／Ⅱ度＝12サイクル中3サイクル欠如／Ⅰ度＝設定PR以上が6連続／ポーズ）、
  **ポーズ基準終了から500ms以内にP波が出てもAAI動作中なのでVペースは追従しない**、
  スイッチバック（12サイクル連続AVD内センス／DDD 100サイクル）、安全機構（翌朝8:00までDDD）、
  設定項目（AVB I switch＝Rest+Exer／Long PR max 350ms・min 250ms／Max pause 3s）
- **`MCP-DPLUSTM`**：名称を「D-Plus™」に訂正。**100サイクルのVペーシング→5サイクル AVD +50ms→8サイクル連続Vsで Pseudo AAI**
- **電池**：MicroPort は**電池内部抵抗の増加を交換指標**にしている。**B.O.S.96／R.R.T.80（10kΩ）／E.O.S.70（>14kΩ）min⁻¹**、
  **3kΩ超で減りが速くなる**、RRT後は VVI 70ppm・**再プログラムしても24時間で復帰**、
  Time to RRT の Min表示（3年未満）と N/A の条件（解析5分未満／リード抵抗200〜3,000Ω外）、表示色4段階
- **製品 931 → 937件。**本体 36 → **40件**（KORA 250 DR TPM010J／SR TPM013J／ENO DR TPM014J／ENO SR TPM015J）、
  リード 868 → **870件**（Xfine JX24D／JX25D。**mri.json にあって製品タブに無かった穴**）。
  既存 Sorin リード12件に MRI 情報を補充（**組合せで判定する旨を明記**）
- **MRI**：`MP-KORA250` の適合リードに固定方式と長さの内訳を追記。**BEFLEX は型番と長さが対応しない**（RF45D＝52cm）ことを備考に明記
- 用語集 **1055 → 1078語**（新規23語＋既存3語を訂正。`WARAD` と `サスピションフェーズ` の `maker` を General → MicroPort CRM に変更）
- `index.html`：`G_FALLBACK` の2語を同期。**陳腐化0件**を確認。453,966 バイト
- **Twin Trace（G＋MVのデュアルセンサ）は新規エントリにしていない**（資料の記載が1行のみ）。notes 追補46に提案として記録

### 2026-09-05（追補45）

リード技術の説明資料（PowerPoint・13スライド）を反映。**用語集と `index.html` のみ変更。**

- **`MIO` の定義を訂正した。**旧「銀イオンによる化学反応／化学劣化」→ 正 **Metal Ion Oxidation（金属イオン酸化）**。
  **MIO は導線コイルとの接触でリードの内側から劣化、ESC（Environmental Stress Cracking）は体内組織の影響で
  主に外側からひび割れる。**`リード絶縁体不良` と `絶縁体損傷` の2語を書き直した
- **シリコンの損傷機序はアブレージョン（摩耗）と裂傷**で、ポリウレタンの ESC／MIO とは別であることを明記
- **絶縁体のトラブルがリード不具合の最大の原因**（絶縁不良が全体の56％。Kleeman ほか、Circulation 2007）
- ポリウレタン vs シリコンの比較（強度・生体安定性・リード径・操作性）、**Optim＝ポリウレタンベースのハイブリッド素材**
- **コネクタ形式12種の一覧**（2012/2013 DATA BOOK）を `旧コネクタ規格（5/6mm）` に全部書き出した。
  製品タブの `connector`（`5mm二股` `3.2mmロープロ` `6mm` など）の値と対応する
- 双極／単極の利点・欠点、同軸構造の **Inner Coil＝カソード／Outer Coil＝アノード**、**導線は3〜4本の多条コイル**
- 用語集 **1040 → 1055語**（新規15語＋既存10語を訂正）。ほかに本文の「障害電流」を「傷害電流」に統一（4語）
- **アルゴリズム 188件・製品 931件・MRI 56グループは変更なし。**資料にアルゴリズム相当の記述・型番情報・MRI 記載がないため
  （`Auto Lead Polarity Detection` は引き続きペーシング管理 残り6件のうちの1件）
- `index.html`：`G_FALLBACK` の10語を同期。**698語すべてを突き合わせて陳腐化0件。**452,952 バイト

### 2026-09-05（追補44）

Abbott のアヴェイル手技資料2点を反映。**4タブすべてに手を入れ、`index.html` の `G_FALLBACK` も同期した。**

資料は①「AVEIR™ DR LP System Procedure Overview（PHYSICIAN TRAINING）」MAT-2500496 v3.0（2026年・37スライド）、
②「アヴェイル VR LP 植込み手順 概要」MAT-2215200 v3.0（2023年）。いずれもテキスト層のあるPDF。

- **［i2i＝4/4］を［i2i＝4/7］に訂正した**（指示）。資料①の［i2i 設定管理］画面から、**設定レベルは 1〜7**、
  方向別（RV→RA／RA→RV）に **i2i スループット（％）と i2i 強度**を自動評価して**推奨レベル**が提示されること、
  **レベル7では電池寿命が短くなる旨の警告**が出ることが判明。§6 の宿題が1件解消した
- **アルゴリズム 187 → 188件。**新規は `ABT-I2I-LINK-EVALUATION`「i2i™ リンクの評価と設定レベル（ペアリング／自動評価）」1件のみ
  （`cat` ペーシング管理。一覧表に行がないため②の形で `equiv` は4社とも「—」）。
  `ABT-I2I-AV-SYNCHRONY` は `how`・`params`・`caution` を訂正・追記
- **i2i の通信条件**：RAO 30° でのボタン間距離 **≦70mm**（心室用デバイス約2つ分）、相対角度 **≦40°**。
  実測例 66mm・3° → V2A 99％／A2V 99％、135mm・84° → V2A 39％／A2V 68％
- **右心房の留置部位**：推奨は右心耳基部（**IDE 植込み全体の61.1％**）。深部右心耳は**脱落率 4.4％ 対 0.9％**、
  中隔は小さく薄く経中隔手技の妨げ、後壁は櫛状筋なし、側壁は下方ほど安定性が落ちる
- **手技の数値**：心房マッピングの目標＝**P波 > 1.0mV・閾値 ≦ 3.0V/0.4ms**、**ロングテザー 33mm**、
  テンションテスト＝ドッキングボタンの動きが**2〜3拍**で収まるまで、心室のディフレクションテスト＝**30〜45°**、
  固定＝1クリック1秒以上で**1.5回転**、フラッシュライン**4本・1滴／1秒**、イントロデューサ**27Fr／25Fr**、
  デリバリーカテーテルは**単回使用で各チャンバごとに新品**
- 用語集 **1020 → 1040語**（新規20語＋既存9語を訂正）。製品は **931件のまま**で6件を訂正・追記
  （`GEAR-PRG-2` に**マーリン プログラマの承認番号 22000BZX00140000** を記入）
- `mri.json` v8 → v9。`ABT-AVEIR` の備考に **「既存心室：LSP202V」の画面表示**を追記（世代の断定はしていない）
- `index.html`：`G_FALLBACK` の5語を同期。**698語すべてを突き合わせて陳腐化0件を確認。**449,601 バイト

### 2026-09-05（追補43）

`index.html` の `G_FALLBACK` を調査し修正。**JSON 4ファイルは変更していない**（`notes.json` への追記のみ）。

- **`G_FALLBACK` は「意図的な抜粋」ではなく「用語集が698語だった時点の全文コピー」と確定した。**
  根拠：`gloss` 先頭698語のうち697語が一致／699語目以降からは0語／並び順が `gloss` の部分列として単調増加。
- **陳腐化16件を修正。**削除済みの `障害電流` を統合後の `傷害電流` に差し替え（削除だけだとオフライン初回訪問時に
  どちらの表記でも引けなくなるため）、内容が変わった15語（`LBBAP` `LVAT` `V6 RWPT` `Aveir VR` `Aveir AR` `Aveir DR`
  `i2i communication` `RRT` `センサ指示レート` `ノイズリバージョン` `デバイスリセット` `リトリーバルヘッド`
  `デリバリーカテーテル（リードレス）` `テンプレート取得` `SVT discriminators`）を現行版に更新。
- **語数は698語のまま。441,348 → 447,734 バイト。`F_FALLBACK`（65行）と `CAT_ORDER` は変更なし。**
  5つの `<script>` ブロックすべてを `node --check` で構文検証済み。
- **全同期（1020語）は見送り。**raw 431→595KB、gzip 126→172KB に対し、`G_FALLBACK` が画面に出るのは
  初回描画の一瞬とオフライン初回訪問のときだけであるため。**手順は notes 追補43に残した。**
- **運用の提案**：用語を訂正・統合・削除したときは `G_FALLBACK` の同名エントリも直す。追加は反映しなくてよい。
  **方針12として §2 に格上げするかは要判断。**

### 2026-09-05（追補42）

Abbott のアヴェイル関連2資料（`Aveir_VR_algorithm.pdf`＝「アヴェイル VR リードレスペースメーカ 院内説明会用資料」
2023年9月13日・95スライド／48ページ・スキャン画像、`_医局医師向け説明会_2025_AVEIRDR_RepToPhy__2_.pdf`＝
「AVEIR DR 医局医師向け説明会」資料）を反映。**4タブすべてに手を入れた回。**

- **アルゴリズムは 187件のまま**（方針11）。**既存9件を拡充した。**
  `ABT-I2I-AV-SYNCHRONY` を全面書き直し（電池寿命表の単位確定、i2i の技術説明、アップグレーダブルシステム、
  ペアリング手順、AR／AR2 の全 LONGEVITY 表、モード選択による差、リアルワールド推定寿命、AR／VR の形状差）。
  `ABT-AUTO-MODE-SWITCH`（AVEIR DR は AMS EGM を最大4つ保存）、`ABT-STORED-EGM`（**AVEIR VR は EGM 非保存**）、
  `ABT-NOISE-REVERSION`、`ABT-MAGNET-RESPONSE`（最初の5拍 VOO/100bpm、RRT で 65bpm）、
  `ABT-VENTRICULAR-INTRINSIC-PREFERENCE`、`ABT-PMT-RESPONSE`（A Pace on PMT）、
  `ABT-ADVANCED-HYSTERESIS`、`ABT-SENSEABILITYTM`（**AVEIR VR に可変センシングは無い**）に追記。
- **§6 の「AVEIR AR2 の比較表の単位」が解消**（`LONGEVITY (YRS)` の明記により「年」で確定）。
- 用語集 **995 → 1020語**（新規25語。リードレス16／レート応答6／植込み・手技1／トラブル1／エビデンス1。
  既存9語をメーカー資料で訂正）。
- 製品 **926 → 931件**（関連機器 22 → 27。アヴェイル デリバリーカテーテル LSCD111／リトリーバルカテーテル LSCR111／
  イントロデューサ LSN25301・LSN25501／リンクモジュール LSL02／心電図ケーブル 3625・3626）。
  **本体4件（AVEIR VR／AR／AR2／DR）を資料で訂正。**AVEIR VR の型番を「AVEIR VR」から **`LSP112V`** に訂正した。
- `mri.json` v7 → v8。`ABT-AVEIR` の `cond` に最大空間勾配磁場 30T/m、静磁場・コイル条件、検査時間の制限なし、
  MRI設定（VOO／MRI基本レート85bpm・30〜120bpm）、**MRIタイムアウト機能が無いこと**、検査前に確認する有害条件6項目を追加。
  **資料上の対象は LSP112V であるため、他モデルへの適用可否を `備考` に安全側で注記した（方針6）。**
- **新規エントリの提案3件（温度センサによるレートレスポンス／EVVI・デバイスリセット／プレマッピング）は
  方針11により追加せず、`notes.json` 追補42に記録した。提案A（温度センサ）は同日に見送りと判断され、用語集での収載が最終形。

### 2026-09-05（追補41）

- **Abbott「AVEIR AR2 セルシート」（2ページ・テキスト層あり／MAT-2510369 v1.0）を反映。**
  **4タブすべてに手を入れた。**`algos.json` v21 → v22（**186 → 187件**）、
  `data.json` v37 → v38（用語集 991 → **995語**）、`products.json` v4 → v5（**925 → 926件**、本体 35 → 36）、
  `mri.json` v6 → v7。**`feats` は未変更**
- 新規1件：**`ABT-I2I-AV-SYNCHRONY`（i2i™ 通信による房室同期／AVEIR DR／AAI(R)＋VVI）**。`cat` は
  **ペーシング管理**（algos に「リードレス」の枠はない）。②の形で他4社「—」。
  **アルゴリズムタブにリードレス関連は1件も無かった穴を埋めた**
- **確定した内容**：**心房用（AR／AR2）と心室用（VR）を i2i™ 導電性通信で連携して DDD(R) を実現／
  AAI(R)＋VVI は心房主体＋心室バックアップで両デバイスの寿命延長が期待できる／
  同条件（300Ω・50bpm・1.5V・0.4ms）で AAI(R)・AAI(R)＋VVI は50％16.1・100％12.4、
  DDD(R)（i2i＝4/4）は50％8.6・100％7.3**
- **AVEIR DR i2i™ 試験**（Knops RE ほか、Circ Arrhythm Electrophysiol 2025;18(4):e013619）の
  **植込み後6〜12か月の平均心房ペーシング率＝洞不全症候群 42.6％／房室ブロック 14.0％**を収載
- **製品に AVEIR AR2（LSP203A・販売名「アヴェイル LP」）を追加。**
  **サイズは従来のままで全ペーシングモードで従来比 約25％の電池寿命向上、
  AAI(R)／AAI(R)＋VVI で10年以上と予測**（1.5V・300Ω・0.4ms・50bpm・心房ペーシング100％）
- **訂正**：`ABT-D-AVEIR-AR` の `model` が「RA2」だったのを **`LSP201A`** に直した（資料に明記）。
  AR／VR／DR の3件に `alias`「アヴェイル LP」を追加
- **MRI**：`mri.json` の `ABT-AVEIR` で **LSP203A の名称を「Aveir AR2」に訂正**し、
  `cond` に備考（**LSP202V が第2世代の心室用かは未確認**）を追加。
  **MRI 条件そのものは添付文書由来で変更していない**（本資料に MRI の記載はない）
- **§6 に2項目を追加**（比較表の単位／［i2i＝4/4］の意味）

### 2026-09-05（追補40）

- **製品タブに `cat`「スリッター・ツール」を新設。**`index.html` の `CAT_ORDER` に
  「デリバリーシース」の直後として追加し、`GEAR-SH-5`（アジリス HisPro スリッター）と
  `GEAR-SH-6`（ヘリックス ロッキング ツール）を移した（`products.json` v3 → v4）。
  **`gear` 22件の内訳はプログラマ6／遠隔モニタリング5／送信機5／デリバリーシース4／スリッター・ツール2**
- **用語集の「障害電流」を「傷害電流」に統合**（992 → **991語**。`data.json` v36 → v37）。
  残した「傷害電流」に**スクリュー固定の組織接触の指標という趣旨**と**LBBAPでの穿孔の手がかり**を追記し、
  `device` を `PM / ICD / CRT / CSP / Leadless` に拡張、`tags` に「障害電流」ほかを追加したので
  **旧表記で検索してもヒットする**
- **`index.html` の `G_FALLBACK`（698語）には「障害電流」が残っている。**
  もともと `gloss` と不一致のため今回は触っていない。同期の可否は次回の判断

### 2026-09-05（追補39）

- **Abbott の LBBAP 関連資料4点を反映**（「左脚領域ペーシング（LBBAP）手技のヒント」2ページ／
  ロケーター 3D 添付文書／UltiPace セルシート／CPS Locator 3D スペックシート）。
  **`data.json` v35 → v36（用語集 978 → 992語）、`products.json` v2 → v3（923 → 925件）。
  `algos.json` は変更なし（186件のまま）**
- **アルゴリズムを増やさなかった理由**：**LBBAP はデバイスのアルゴリズムではなく植込み手技**で、
  資料の中身はターゲット部位・ペースマッピング・リードの回し方・捕捉の判定・PSA測定値。
  `scope`／`how`／`params` の形に落ちない（方針11）
- **ただし2点だけプログラミングの記載がある**：**狭いQRSを達成するには PAVD／SAVD の最適化が必要**、
  **バイポーラで3.5〜5Vの高出力を用いると陽極刺激が観察されV1のR波終末が低減することがある**。
  **新規エントリの提案として notes 追補39 に記録し、今回は用語集に収めた**（設定範囲が資料に無いため）
- **既存3語をメーカー資料で訂正**：**LVAT＝V5またはV6でR波ピークまで計測し80ms未満が目安・高低出力で一貫**、
  **V6 RWPT＝V1はRV中隔／V6は外側LVを観察し、V6−V1差が44ms超でセレクティブ・33ms超でどちらか**、
  **LBBAP＝RAO30度でヒス電位から心尖方向15〜35mm・三尖弁輪の中隔尖の下・LAO30度で中隔に垂直**
- **確定した数値**：**ペースマッピングはVVIで固有心拍＋10〜15bpm・5Vまたは7V・0.4/0.5ms／
  目標波形はII誘導＞III誘導の陽性成分・aVR陰性かつaVL陽性・V1でノッチのあるQS（右室流出路起源はaVLもaVRも陰性）／
  ヘリックスは Tendril™ STS 2088TC で時計回り8〜18回転、固定後さらに5回転／
  PSA目標は閾値0.5msで1.5V未満・バイポーラセンシング4mV超／
  低出力でRWPTが15ms超延長したらセレクティブ捕捉が消失**
- **穿孔の手がかり**：COIの大幅な変化・消失、単極インピーダンスの大幅な低下、
  **先端ユニポーラのCOIが陰性**、**リングのCOIが先端より大きい**
- **ロケーター 3D（`GEAR-SH-3`）を全面的に書き直した**：**型番5種（DS2C100-42／DS2C200-42／DS2C300-42／
  DS2C200-45／DS2C300-45。Extra Long は Medium と Large のみ）／内径7F（2.44mm）・外径9F（3.00mm）／
  有効長42cm（全長45cm）・45cm（全長48cm）／6Fリード適合でリード長58cm以上・65cm以上を推奨／
  ダイレータは0.035インチGW対応**。**禁忌・警告（MRI環境下では使用しない、造影剤等の高圧注入禁止ほか）を
  `role` に収載**。一般的名称は**心臓用カテーテルイントロデューサキット（JMDN 10598000）**
- **製品を2件追加**（`gear` 20 → 22）：**アジリス HisPro スリッター（Model DS3A001）**と
  **ヘリックス ロッキング ツール（HLT）**。**`cat` は暫定で「デリバリーシース」**
- **UltiPace（`ABT-L-ULTIPACE`）を補強**：**型番 LPA1231／イントロデューサ6F／
  遠位部の断面は Tendril™ STS と同一で、近位部にPTFEの内側・外側ライナーを追加／
  チップ電極からリング電極まで14.5mmの剛体部／絶縁体を足しても6Frのまま**。
  比較データ（Tendril STS比で摩耗3倍・損傷3.5倍、Tendril MRI比で2.2倍・2.6倍）は
  **リードに自由記述欄がないため用語集「UltiPace™ リード」に収めた**
- **重複に気づいた**：用語集に **「障害電流」と「傷害電流」が別々に存在する**（内容はほぼ同じ）。
  統合の可否は保留
- 収載しなかったもの：**コネクタピン変換アダプタ**（販売名と承認番号しか資料に無い）、
  **リードの承認番号**（`approval` 欄が無いため notes にのみ記録）、**LBBAPの引用文献4件**

### 2026-09-05（追補38）

- **St. Jude Medical 名義の資料2点を反映。アルゴリズム 184件 → 186件**（`algos.json` v20 → v21）。
  用語集 948 → 978語（`data.json` v34 → v35）。**`feats` は未変更なので `F_FALLBACK` の同期は不要**
  ①「Stored Electrograms」（30ページ2アップ・59スライド。**前のトークで添付されていたが未反映だったもの**）
  ②「CRT植込み手順」（38ページ1アップ。**指示により補足として使える箇所だけを収載し、CPS関連製品は登録しない**）
- **保留していた `cat` の判断を実行**：**「心不全」を「モニタ」に改称し、OptiVol／CorVue／ST モニタリング／SAM の
  4件を移した。「心不全」という `cat` は消滅**。新規のエクササイズ＆アクティビティ診断を加えて **モニタ 5件**。
  **`index.html` は改修していない**（`uniq(all,"cat")` で動的生成のため）
- 新規2件：**ストアード EGM**（`cat` ICD）／**エクササイズ＆アクティビティ診断**（`cat` モニタ）。いずれも②の形
- **ストアードEGMの設定が確定**：**容量は 1ch 45分／2ch 22分／3ch 15分（ノミナル2ch）／
  極性のノミナルは［センス アンプ］＝ICDが検出に用いるEGMそのもので、他の極性を選んでも検出には影響しない／
  リードレスECGは DRモデル A チップ to RV コイル、VRモデル SVC to CAN／
  VT/VF EGM 最長1分・プレトリガ最長14秒 → プレ14秒＋ポスト46秒**
- **EGM ステッチングは Current™・Promote™ 以降。**サイナス復帰したところまで記録される。
  **Atlas®Ⅱ までは「サイナス復帰から4秒後」または「設定最長保存時間の完了」で終了し FIFO**
- **VT/VF 以外のトリガの保存時間は変更不可**（プレ／ポスト秒）：**AMS 10/6・AT/AF 12/4・PMT 12/4・
  ノイズ リバージョン 10/6・マグネット 10/6・モーフォロジー 20/4・PCショック 12/4**
- **エピソード トリガは7項目・優先度 Off／Low／High（VF は Low 不可）。保護されるのは
  VT/VF/PMT/マグネット＝最初、心房＝最新・最初・最速レート、モーフォロジー＝最新、PCショック＝最新。
  上書きは ①保護 → ②VT/VFエピソード → ③直近のHigh の順**
- **エクササイズ＆アクティビティ診断**：**エクササイズ＝1分以上の活動／最大心拍数＝220−年齢（生年月日未入力なら
  60歳で計算）／目標心拍数は最大心拍数の％で設定／収集は1分以上で開始・2分の活動停止で終了／
  アクティビティの再較正は開始したら中止できない**
- 既存14件を補強（`ABT-ALERT-TRIGGER`／`ABT-SHOCKGUARDTM-DETECTION`／`ABT-RATE-BRANCH`／
  `ABT-AUTO-MODE-SWITCH`／`ABT-MORPHOLOGY`／`ABT-ANTITACHYCARDIA-PACING`／`ABT-HIGH-VOLTAGE-THERAPY`／
  `ABT-CAPACITOR-MAINTENANCE`／`ABT-PMT-RESPONSE`／`ABT-NOISE-REVERSION`／`ABT-MAGNET-RESPONSE`／
  `ABT-LEAD-MONITORING`／`ABT-DEFT-RESPONSETM`／`ABT-ST-MONITORING`）。
  **診断データ側の集計（エピソード ツリー・SVT診断サマリー・治療サマリー・HVチャージ・AMSログ）を
  各機能に結び付けたのが今回の主眼**
- **NSVT・NSVF の定義が確定**：**トリガしたが治療にいたらなかったエピソード（チャージ中に停止）**
- **AMS ログは合計32イベント（16固定＋16 FIFO）。タブ名は AMS On で【モード スイッチ】、Off で【AT/AF】。
  心房レート ヒストグラムはAMS中も反映されるが、イベント ヒストグラムと【房室伝導】タブは含まない**
- CRT植込み手順から：**CSリードの最終測定極性**（IS-1／DF-1・DF-4 は3極性、**Anthem™ は LVtip-CAN を含む4極性**、
  **Frontier™Ⅱ は LVring-RVring が使えず3極性**）を `ABT-AUTO-VECTSELECT-QUARTETTM-TEST` に、
  **植込み時のPSA測定順序**（**CRTでは横隔膜神経刺激の確認（5V）をペーシング閾値測定より先に行う**）を
  `ABT-LVCAPTM-CONFIRM`／`ABT-BIVCAPTM-CONFIRM` に収載。**空欄だった `caution` が3件埋まった**
- **植込み時のデバイス チェックの目安**を `ABT-CAPACITOR-MAINTENANCE` に収載：
  **キャパシタ メンテナンス ＜15秒／Promote+ バッテリー電圧 ＞3.15V／Unify™ 電池残容量 ＞90％**
- **サイズ換算表を用語集に3語で収載**（フレンチサイズ＝1Fr 1/3mm・ゲージは数字が大きいほど細い・
  1インチ＝25.4mm）。**7Fr内腔に6Fr子カテを入れると差は0.33mmで 0.014インチ（0.36mm）は同時に通らない**
- **§6 に1項目を追加**（［診断］と［検出］）、**2項目の状況が変わった**
  （キャパシタ最大電圧＝830V の傍証／STエピソードEGMの保存規則＝一般ルールで説明可）
- **収載しなかったもの**：CPS関連製品（指示による）、PDFレポート出力とデータ消去（プログラマ機能）、
  プログラマ操作経路（方針9）。詳細は notes 追補38

### 2026-09-05（追補37）

- **St. Jude Medical 名義「ST Monitoring」（ST モニタリング専用資料・28ページ2アップ・55スライド）を反映。
  件数は 184件のまま**（`algos.json` v19 → v20）。用語集 933 → 948語（`data.json` v33 → v34）。
  **`feats` は未変更**
- **`ABT-ST-MONITORING` を全項目書き直した。**追補31では別資料の断片から起こしたエントリだった
- **測定の基本が確定**：**極性 Can(−) to RV Tip(+)／90秒ごとに15心拍のVsを解析（ST変化時は30秒ごと）／
  等電位インターバル＝Vsの手前部分、STインターバル＝Vsの後ろ部分（いずれも設定変更可能）／
  ベースラインは6時間ごとに取得しその4つの平均を0mVとする**
- **植込みからの流れが確定**：**4時間後に収集開始（傷害電流・ポケット内不安定のため）→
  安静時ゾーン内Vs 4060拍＋96時間でベースライン完成・閾値を自動算出（3σ）→
  1週間後に閾値を設定してからSTエピソードログの収集が始まる。**
  **7日を超えた場合は直近7日間の安静時ゾーン内Vs全てで算出**
- **心拍数ゾーンが確定**：**4ゾーン。最下限＝基本レートまたは40min⁻¹、最上限＝VT検出レートまたは140min⁻¹。
  設定変更できるのは安静時ゾーンの上限のみ（ノミナル86min⁻¹）で、上の3帯は均等分割**
- **上昇／下降の閾値が確定**：**％表示で、ベースラインのR波高値（base to peak）を100％とする。
  デバイスが自動算出するが、推奨値は手動でプログラミングしないと反映されない**
- **Enhanced ST Monitoring（Ellipse™ 以降）のタイプ分けを収載。
  タイプ1＝臨床的な意味が高い、タイプ2＝臨床的な意味が低い可能性がある。
  通知のノミナルはタイプ1が ペイシェント ノーティファイア OFF／FastPath™ ON／Merlin.net™ URGENT、
  タイプ2が FastPath™ ON のみ**
- **訂正1：追補31の「閾値を超えると Patient Notifier が作動して患者に知らせる」は誤り。
  患者通知のノミナルは OFF。**
- **訂正2：追補31の「1プロットは90秒ごとの6拍平均」は誤り。90秒ごとに15心拍を解析する。**
  ST偏差は6か月表示で1プロット1週間分、1か月表示で1日分
- **使用上の注意を収載**：**ST Monitoring を使うとVT/VFエピソードEGMの取得可能チャンネルが2chになる。**
  うまく記録できない4ケース（心室ペーシング率が高い／AFバーデンが高く**心房レートATDR以上では解析しない**／
  日常のHRが安静時ゾーン以上／DDDで心房ペーシング率が高い）
- **セットアップ手順と擬陽性対策を `pitfall` に収載**（STフリーズはHRが安静時範囲にないと取得できない、
  STインターバルがQRSに掛かっていたらずらす、**セットアップ段階で閾値は触らない**）
- **`ABT-ALERT-TRIGGER` を補強**：STエピソードアラートの通知先ノミナルと、**Merlin.net™ PCN の URGENT 区分**
- **§6 に2項目を追加**（STシフトの判定基準の分母、STエピソードEGMの保存規則）

### 2026-09-05（追補36）

- **St. Jude Medical 名義の資料2点を反映。アルゴリズム 183件 → 184件**（`algos.json` v18 → v19）。
  用語集 918 → 933語（`data.json` v32 → v33）。**`feats` は未変更なので `F_FALLBACK` の同期は不要**
  ①「Ellipse™ ICD が提供する解決策」（8ページ2アップ・16スライド。DynamicTx™／パリレンコーティング／SecureSense™）
  ②「Unify™ CRT-D and Fortify™ ICDs ─ CorVue™ モニタリング」（24ページ2アップ・47スライド。CorVue専用資料）
- 新規1件：**アラート トリガ（FastPath™ 表示／ペイシェント ノーティファイアー）**。**①の形**で
  一覧表41行目「患者通知機能（本体の音）」の穴を埋めた（**3社ぶん値があるのにアルゴリズムは5社とも0件だった**）
- **`ABT-CORVUETM` を全項目書き直した（`ecg` が埋まり未着手が解消）。**
  **2時間ごとに測定・日の平均値／測定はRVコイル〜本体とRVリング〜本体（インテグレーテッドはコイルのみ）／
  リファレンス作成は ICD 12日・CRT-D 14日でこの間データ非表示／植込み後30日は使用しない／ノミナルOFF／
  ［パラメータ］⇒［診断機能］タブ**
- **CorVue の「1日」の定義が判明**：**インデックス12カウントで1日。実測＜リファレンスで+1、≧で−1。
  暦の1日とは一致しない。トリガ日数×12に達し、さらに12回連続（24時間）増加してアラート**
- **CorVue トリガ日数：8〜18日、ノミナル CRT-D 14日／ICD 13日。**
  **青バー＝プレトリガ、橙バー＝ポストトリガ。7日（インデックス84）未満は青バーで表示されない。
  エピソードは20件 First-in First-out・データは1年分。臨床コメントは最大5件**
- **測定が行われない9つの状況**（頻拍Binned中・ショック後ペーシング中・各インピーダンス測定中・
  キャパシタチャージ中・ノイズリバージョン中・患者通知中・テレメトリー中・テンプレートメンテナンス中）と、
  **未測定時は5分後に最大3回（15分）再試行、HV治療後は1時間後に再試行**を収載
- **`ABT-DYNAMICTXTM-OVER-CURRENT-DETECTION` を大幅補強。**
  **OCDは Current™ ICD／Promote™ CRT-D（本邦2009年発売）以降に搭載。DynamicTx は Ellipse™ で導入。**
  作動例4ステップ（**OCD検出時は通常0.3J程度で通電終了／極性変更後の出力は設定に関わらず最大出力**）、
  **使用条件（デュアルコイル＋SVCコイルON。リード種類・コネクタ形状は問わない。シングルコイル不可）**、
  **極性変更はOCDが働いたときのみで、HVLICによる低抵抗検出では極性は変わらない**
- **`ABT-LEAD-MONITORING` の空欄 `caution` が埋まった**（HVリード インピーダンス範囲外通知 20〜125Ω、HVLIC）。
  **これで Abbott の ICD 残りは VF Therapy Assurance 1件だけ**
- **`ABT-SECURESENSETM-RV-LEAD-NOISE` を訂正・補強**：**ファーフィールドは「RVコイル−Can もしくは RV Ring−Can」の2通り。**
  感度・特異度の分母分子が確定（VF 853/853、持続性ノイズ 231/238、非持続性ノイズ 47/52。HRS 2012）
- **`ABT-DEFT-RESPONSETM`：DynamicTx™ アルゴリズムのノミナルが ON と確定**（画面例も追加）
- **`ABT-SHOCKGUARDTM-DETECTION`：2ゾーン画面例 VT 16インターバル／VF 12インターバル。§6 の判断材料が増えた**
- **Ellipse™ ICD 4機種のスペックとパリレンコーティングを追補36に記録**（製品タブ用）

### 2026-09-05（追補35）

- **St. Jude Medical 名義「Arrhythmia Therapy」（頻拍治療の専用資料・26ページ2アップ・52スライド）を反映。
  アルゴリズム 181件 → 183件**（`algos.json` v17 → v18）。用語集 903 → 918語（`data.json` v31 → v32）。
  **`feats` は未変更なので `F_FALLBACK` の同期は不要**
- 新規2件：**ATP（抗頻拍ペーシング）／高電圧治療（CVRT／Defibrillation）**。いずれも②の形、他4社「—」。
  **ATPは用語集に語だけあってアルゴリズムの実体が無かった穴を埋めたもの**
- **ATPの設定値が全て判明**：バースト回数1〜15（ノミナル3）／刺激回数2〜20（ノミナル8）／
  追加刺激On・Off（ノミナルOff・全20刺激まで）／BCL＝検出時のIA×％（420ms×85％＝357ms）／
  Min BCL 200ms／リアダプティブOn・Off（**ノミナルOff**）／スキャンステップ**ノミナル12ms**／
  最大ステップ**ノミナル50ms・リアダプティブOnで設定可**／ランプステップ**ノミナル10**／
  **ATPパルス振幅7.5V・パルス幅1.0または1.5ms**
- **ATPの同期が判明**：1拍目は**バイポーラEGMの下行脚に同期**（体表面ECGのQRS onsetの40〜80ms後）、
  **2拍目以降はVOO**。**再検出には6拍のVTインターバルが必要**。
  **エピソードデータには最初と最後のATPサイクルしか表示されない**
- **高電圧治療の定義が確定**：**CVRT＝VTへの低出力通電（最高36Jまで）／Defib＝VFへの高出力通電。
  どちらもR波に同期。**チャージ終了後**一拍飛ばし、次の Non-Sinus Complex に同期**（「Sinus」「Not Binned」は除外）。
  **チャージ開始後6拍のNon-Sinus Complexの確認が必要。サイナス再検出でAborted。全ショックがNon-Committed**
- **`ABT-CAPACITOR-MAINTENANCE` を全項目書き直した**（空欄だった `ecg` が埋まった）。
  Deformationの定義（酸化アルミニウム被膜→アルミニウム）、手動実行は**ダンプ＋最大電圧チャージ→約1分**、
  **残存電圧は約1時間かけて消散**、**Atlas+Ⅱまではダンプ項目あり・Current™から自動ダンプ**、
  **チャージインターバルを変えても古い設定のメンテナンスが実施されるまでタイマーはリセットされない**
- **`ABT-DEFT-RESPONSETM` を補強**：**［チルト固定］がNominal設定でCVRTとDefibを別個に設定できる。
  パルス幅で設定してTiltを自動調整できるのはSJMだけ**。**二相性はDFTが30％向上・Ventritexが世界初の実用化**。
  右室極性の表記を資料どおり**「右室(+) to CAN(−) ⇔ CAN(+) to 右室(−)」**に訂正
- **訂正：`ABT-ATP-PRIOR-DURING-CHARGING` の「6拍の頻拍を確認」は「6拍の Non-Sinus Complex を確認」が正しい**
- **`ABT-VT-THERAPY-TIMEOUT`：［Off］が選べることが確定**（このとき［タイムアウト トリガ］は n/a）。
  範囲とノミナルは依然未確認
- **§6 に2項目を追加**（キャパシタの最大電圧 830V/845V、メンテナンス間隔の自動変更 2.86V/2.6V）
- **SJM ICD のバッテリー情報を追補35に記録**（リチウム銀酸化バナジウム3.2V、2.60〜2.55Vでフラット、
  2.60Vで全寿命の6割経過、ERI 2.45V）。製品タブが動いたら使える

### 2026-09-05（追補34）

- **St. Jude Medical 名義「Arrhythmia Detection」（頻拍検出の専用資料・22ページ2アップ・
  2009.7.13 Current Version）を反映。件数は 181件のまま**（`algos.json` v16 → v17）。
  用語集 896 → 903語（`data.json` v30 → v31）。**`feats` は未変更**
- **訂正1：サイナス再検出の「連続カウント」は誤りだった。**追補32で手書きメモとして収載保留にしていたが、
  本資料の本文は「**連続である必要はないが "F" "T2" "T" の Binned でリセットされる**」と明記
- **訂正2：検出インターバル数が資料間で食い違う**（§6 に追加）
- **`ABT-SHOCKGUARDTM-DETECTION` を大幅に補強。****治療後のレート変化の4パターン**
  （サイナス復帰／同ゾーン継続＝再検出基準／速いゾーンへ移行＝新ゾーンの初回検出基準／
  **遅いゾーンへ移行＝初回治療より弱い治療にはならない**）、
  **ゾーン間の30ms制約**、**［ゾーン設定：Off］の意味と［前回設定の復帰］**、
  **再検出レートの選択肢**（VF＋50ms／VT＋30ms）、**DCは最大6回・ATPの後はCVRTかOffのみ・
  モニタはVT-2に設定できない**、**マーカ T／T2／F** を収載
- **`ABT-VT-THERAPY-TIMEOUT`**：画面例 **20分**・タイムアウトゾーン VT-2。範囲とノミナルは依然未確認

### 2026-09-05（追補33）

- **St. Jude Medical 名義「Sensing / SenseAbility™」（SenseAbility 専用資料・22ページ2アップ・
  2009.7.13 Current Version）を反映。アルゴリズム 180件 → 181件**（`algos.json` v15 → v16）。
  用語集 890 → 896語（`data.json` v29 → v30）。**`feats` は未変更なので `F_FALLBACK` の同期は不要**
- 新規1件：**PVAB（心室後心房ブランキング）**。60〜250ms・ノミナル60ms・**Off にできない**。
  **Atlas+Ⅱ までの名称は「Far Rサプレッション」**
- **`ABT-SENSEABILITYTM` を全項目書き直した。**搭載機種が **Photon® DR 以降のSJM・ICD全機種**と確定し、
  正式名称 **ASC（Auto Sensitivity Control）**、**デジタル整流波形（Digitally Rectified Signal）**、
  **ペーシング後パラメータ一式**（Threshold Start は mV 指定・Pace Refractory ノミナル250ms）、
  **心室ペーシング後［Auto］の中身**（QT間隔短縮を模倣してレート上昇に応じ Threshold Start を下げ
  Decay Delay を短縮）が判明
- **訂正：Decay Delay は 0／30／60／95／125／160／190／220ms の8段。**追補32までの「90」は誤りで
  「160」が欠落していた。**§6 の「Decay Delay の4段目」の項は解決したので削除した**
- **Threshold Start のノミナルが確定**（心室62.5％・心房50％）
- **センシング不応期の設定場所は Current™ から変更**され、徐脈タブの［不応期 & ブランキング］にある
- **新たな食い違い**：最高感度のノミナルが資料間で違う（§6 に追加）

### 2026-09-05（追補32）

- **St. Jude Medical 名義「SJM デバイスアルゴリズム」（Merlin™ PCS の画面に沿った ICD/CRT 解説・22ページ2アップ）を反映。
  アルゴリズム 176件 → 180件**（`algos.json` v14 → v15）。用語集 869 → 890語（`data.json` v28 → v29）
- 新規4件：**ShockGuard™（頻拍検出：ゾーンと Binning）／エピソーダルペーシング／ショック後ペーシング／
  キャパシタ メンテナンス**
- **`ABT-ATP-PRIOR-DURING-CHARGING` と `ABT-DYNAMICTXTM-OVER-CURRENT-DETECTION` の空欄が埋まった。**
  前者は VFゾーンATP の上限レート（300min⁻¹まで・ノミナル250min⁻¹）とチャージ中の検出（6拍確認・
  終了後1拍無視・サイナス再検出で中止）、後者は **60A の過電流検出（OCD）と極性の自動変更
  （RV−SVC/CAN → RV/CAN → RV/SVC）**
- **`feats` 62行目「ショック後・治療後のペーシング」の Abbott 欄を埋めたので `F_FALLBACK` を同期**（方針8）。65行の完全一致を確認済み
- **`ABT-INTERVAL-STABILITY` のノミナルが確定**（On w/AVA・デルタ40ms・AVA 60ms・ウィンドウ12）。§6 から1項目を削除
- **SenseAbility の「1mV／312ms」の正体が判明**（自動感度の減衰の傾き）。Threshold Start のクランプルールと
  最高感度（心房0.3／心室0.5mV）も収載
- **旧機種SVT識別の世代表が判明**：Atlas®/Epic™ → Current™/Promote™ → Unify™/Fortify™ → Ellipse™ Limited →
  New Ellipse™/Assura™。FFMD とチャンバーオンセットは **Ellipse™ Limited 以降**で追加・ノミナル
- **DeFT Response のショック設定ルール**（最大出力を初回治療に設定できない。前の治療が30J以上であることが条件）
- 手書きメモにしかない情報（サイナス再検出の連続カウント、Binning のカウンタリセットなど）は**収載せず追補32に記録**

### 2026-09-04（追補31）

- **St. Jude Medical 名義「Tachy SVT Discriminator ②」（Ellipse™ ICD 資料・43ページ2アップ）を反映。
  アルゴリズム 174件 → 176件**（`algos.json` v13 → v14）
- 新規2件：**低周波減衰**（一覧表27行目「T波識別機能」の Abbott 欄が指していた実体。①の形で `equiv` を生成）、
  **ST モニタリング**（`cat` は暫定で「心不全」）
- **`ABT-SECURESENSETM-RV-LEAD-NOISE` の空欄 `caution` と `ABT-DEFT-RESPONSETM` の空欄 `ecg` が埋まった**
- **`ABT-SENSEABILITYTM` の設定値がすべて判明**（Threshold start 50／62.5／75／100％、
  Decay Delay 0〜220ms の7段、Sensed Refractory Period 93／125／157ms）
- **ファーフィールドMDのテンプレートの作り方が判明**：8拍平均を3時間ごと×16セット＝48時間分を平均。
  **RV Tip-Ring EGM は判定ではなく「時相を揃える基準点」に使う**
- **チャンバオンセットの判定7ステップ**（変化の前後で5拍のCIを検証する）を収載
- **AVアソシエーションデルタのノミナル 60ms が確定**（§6 から削除）。
  モーフォロジータイプのノミナルの食い違いも「世代差」で決着（Far Field MD は Ellipse で追加）
- 用語集10語を追加（859 → 869語）。`feats` は未変更

### 2026-09-04（追補30）

- **St. Jude Medical 名義「Tachy SVT Discriminator ①」（Current™ 世代・45ページ2アップ）を反映。
  アルゴリズム 171件 → 174件**（`algos.json` v12 → v13）
- 新規3件：SVT アッパーリミット／二段脈アボイダンス／VT治療タイムアウト
- **レートブランチに「鑑別は最初の検出時のみ有効・治療開始後は作動しない」**と、
  SVT識別後も6心室イベントごとに検出を継続する仕組み（7つの終了条件）を収載
- **モーフォロジーのテンプレート自動更新の全手順**（6/8・80％、仮採用、8時間後の再判定、
  キャンセル条件4つ）と、**テンプレート未取得だとスコアリングが全て0％になる**という要注意を収載
- **サドンオンセットの比較基準を訂正**：「ノンサイナスと判定された拍のIA」ではなく
  **「VTゾーンに入った時のIA」を直前8拍のうち2・4・6・8拍前のIAと比較**
- **SVT ディスクリミネーション タイムアウト**にタイマー起点・タイムアウト後治療（ノミナルVF治療）・
  **Atlas® までの世代はノミナル30秒**を追加
- 用語集12語を追加（847 → 859語）。`feats` は未変更なので `F_FALLBACK` の同期は不要
- **2アップのスキャンPDFは同じページ数で倍のスライドが読めると実測**（§4 を更新）

### 2026-09-04（追補29）

- **Abbott「ICD 1 勉強会」の SVT ディスクリミネータの章を反映。アルゴリズム 170件 → 171件**（`algos.json` v11 → v12）
- 新規1件：SVT ディスクリミネーション タイムアウト。**`feats` 61行目の Abbott 欄「—」を埋める形**で追加した
- **`feats` を変更したので `index.html` の `F_FALLBACK` を同期**（方針8）。65行の完全一致を確認済み
- SVT鑑別6件を全面的に補強。**レートブランチの中央値の取り方（8〜16インターバル・ノミナル12）と
  V＝A の定義（差 30ms 以内）が確定**
- **訂正**：サドンオンセットの `params` は「Onset率（％）」ではなく、
  **［固定］30〜500ms（ノミナル100ms）／［アダプティブ］4〜86％（ノミナル18％）の二者択一**。
  比較対象も「直前の平均」ではなく **2・4・6・8拍前の IA との差の最大値**
- **モーフォロジー2件の役割を分けた**（`ABT-MORPHOLOGY`＝総論＋オリジナルMD、
  `ABT-FAR-FIELD-MDTM-MORPHOLOGY`＝ファーフィールドMD）。従来は `ecg`・`pitfall` が同一文だった
- **チャンバオンセットの `caution` と `pitfall` の重複を解消**し、8秒／40ms の分岐アルゴリズムを収載
- 用語集21語を追加（826 → 869語）。既存の `SVT discriminators` の説明を資料準拠に訂正
- **テキスト層のあるPDFだったため100ページ超でも全文を読めた**（§4 の表を実測で更新）

### 2026-09-04（追補28）

- **「モード関連Ⅰ」（100ページ）を反映。アルゴリズム 163件 → 170件**（`algos.json` v10 → v11）
- 新規7件：サーチ機能付ネガティブAVヒステリシス／レート応答AVディレイ／レート応答PVARP・V Ref／
  心室セーフティスタンバイ／心室ブランキング（Auto）／ノイズリバージョン／マグネットレスポンス
- **訂正**：レストレートの搭載機種に **Accent** を追加（設定範囲 30〜170min⁻¹）。追補26の未確定が解決
- PVCレスポンス・PMTレスポンスに画面名・PMT検出レートの値・**VA伝導の頻度と確認方法**を追加
- **マグネットレート対応表（Accent・Zephyr）が取れた。**製品タブは保留中のため追補28に記録のみ
- 用語集12語を追加（814 → 826語）。`feats` は未変更なので `F_FALLBACK` の同期は不要

### 2026-09-04（追補27）

- **`pathway` を全件削除。10／163 → 0／163**（`algos.json` v9 → v10）。フィールドごと削除
- 理由：画面写真からの読み取りでは世代差・バージョン差を追い切れず、誤った経路は信頼性を損なう
- **削除した10件の値は追補27に原文で記録。**`index.html` の実装は残っているので、戻せば再表示される
- Merlin™ PCS の画面構成の記録（§4）は残した。方針9を「当面埋めない」に書き換えた

### 2026-09-04（追補26）

- **提案1〜3を採用し、アルゴリズムを 160件 → 163件に増やした**（`algos.json` v8 → v9）。
  `ABT-PVC-RESPONSE`／`ABT-PMT-RESPONSE`／`ABT-REST-RATE`。Abbott は 30 → 33件
- 操作経路 7→10／163（追加3件すべてに経路が入った。**追補27で全件削除**）
- `feats` は変更していないので `F_FALLBACK` の同期は不要。`data.json` も未変更
- `equiv` は登録済みの内容だけで作った。PMTレスポンスとレストレートは他4社すべて「—」
  （**非搭載の意味ではない**）。レストレートの他社名称は Abbott 資料由来のため入れていない
- 提案4（レート応答・センサ）は指示の対象外のため見送り

### 2026-09-04（追補25）

- **アドバンストヒステリシスを詳細化。操作経路 6→7／160**
- **訂正**：`params` は「On／Off；介入レート；介入時間」ではなく、［レート］画面の
  【ヒステリシス設定】6項目（ヒステリシスレート／サーチ間隔／サイクル数／インターベンションレート／
  持続時間／リカバリータイム）。単独の On／Off スイッチはない
- **訂正**：VIP の操作経路の頭に「パラメータ ＞」を追加（パラメータ画面のタイル構成が確認できたため）
- Merlin™ PCS の画面構成が確定（上表）
- PVCレスポンス・PMTレスポンス・レストレート・レート応答は**入り先がないため新規エントリの提案として記録**
- 用語集13語追加（801→814語）

### 2026-09-04（追補24）

- VIP と QuickOpt を詳細化。**操作経路 4→6／160**
- **訂正**：VIP の作動基準は3通り（サーチ中＝設定サイクル数中1拍でも自己収縮／
  サーチ外＝3拍連続・設定と無関係／解除＝設定サイクル数と同数の連続Vp）
- VIP が無効になる5条件、QuickOpt の算出式（SAVopt／PAVopt）を収載
- 用語集9語追加（792→801語）
- **HANDOFF の件数誤りを訂正**：Abbott の ICD は11件ではなく**12件**

### 2026-09-04（追補23）

- 心室オートキャプチャーと ACap コンファームを詳細化。**操作経路 2→4／160**
- **訂正**：ACap のバックアップパルスは「捕捉が確認できない拍」ではなく、
  Accent／Anthem では**全テストパルスに対して30ms後にバイポーラ5.0V**で入る
- ER検出方式（PDI／DMAX）、Kendall Tau相関、1.7倍マージン表、フュージョン回避を収載
- 用語集7語追加（785→792語）

### 2026-09-03（追補22）

- Auto Mode Switch と AF Suppression を詳細化。**操作経路が初めて埋まった（0→2／160）**
- **訂正**：AF サプレッションの起動条件は「2拍連続」ではなく
  **「16サイクル以内にASが2回」**（連続である必要はない。確認サイクルは16拍固定）
- FFRWS を Type I／II で書き分け（Type II は PVAB では対処できない）
- 用語集10語追加（775→785語）
- **HANDOFF の件数誤りを訂正**：Medtronic ペーシング管理は12件ではなく**9件**

### 2026-09-02

- アルゴリズム：指定138機能を収載 → 機器系31件を製品タブへ移設 → 26件削除 → **160件**
- 一覧表：102行 → **65行**に整理。他社類似機能の生成を一覧表基準に変更
- 製品：本体詳細を i pacemaker 相当に再設計（寸法／コネクタ／MRI／電池／通信／診断）
- リード：製品名を英語ブランドに統一、MRIセクションを追加
- 添付文書7点を反映。**RESONATE の DF-1モデルは1.5Tのみ**という安全上の訂正を実施
- 5メーカーの薬事情報を一巡（承認番号は表示から削除済み、国内発売は承認日・保険適用日のみ）
